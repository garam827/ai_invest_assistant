# 프로젝트 명세서: 톰 바소(Tom Basso) 스타일의 추세추종 투자 어시스턴트 챗봇 (최종 확정안)

본 문서는 전설적인 트레이더 톰 바소의 투자 철학(기계적 시스템, 규칙 기반 대응, ATR 기반 변동성 관리 및 포지션 사이징)을 반영한 LLM 기반 투자 어시스턴트 챗봇을 **Vibe Coding(프롬프트 기반 AI 코딩)**으로 구축하기 위한 최종 마스터 가이드라인입니다.

---

## 버전 이력 (Changelog)

| 버전 | 날짜 | 변경 내용 |
| --- | --- | --- |
| v1.0 | (최초 작성) | 최초 확정안. LLM 엔진: GCP Vertex AI (Gemini 1.5 Pro). 전체 내용은 [investment_assistant_spec_v1.md](spec_archive/investment_assistant_spec_v1.md) 참고 |
| v2.0 | 2026-07-18 | **LLM 엔진을 GCP Vertex AI(Gemini)에서 OpenRouter로 변경** (비용 절감 목적). 기본 모델: `nvidia/nemotron-3-nano-30b-a3b:free`. 이에 따라 인증 방식(서비스 계정 → API 키)과 관련 섹션(1.1, 1.2, 기능3) 갱신. 전체 내용은 [investment_assistant_spec_v2.md](spec_archive/investment_assistant_spec_v2.md) 참고 |
| v2.1 | 2026-07-18 | **구글 드라이브 인증 방식을 서비스 계정 키 → OAuth 사용자 인증으로 변경.** 조직 정책(`iam.disableServiceAccountKeyCreation`)이 서비스 계정 키 발급 자체를 차단하는 GCP 프로젝트여서, 서비스 계정 키 대신 OAuth 클라이언트(데스크톱 앱)로 사용자 본인 계정에 동의를 받는 방식으로 전환. 설정 절차는 [GOOGLE_DRIVE_SETUP.md](GOOGLE_DRIVE_SETUP.md) 참고. 전체 내용은 [investment_assistant_spec_v2.1.md](spec_archive/investment_assistant_spec_v2.1.md) 참고 |
| v2.2 | 2026-07-18 | **OAuth 인증 트러블슈팅 추가**: OAuth 동의 화면이 "테스트" 상태일 때 로그인 계정이 "테스트 사용자" 목록에 없으면 `액세스 차단됨: ...은(는) Google 인증 절차를 완료하지 않았습니다` 오류가 발생함을 [기능 1]과 [GOOGLE_DRIVE_SETUP.md](GOOGLE_DRIVE_SETUP.md)에 명시. 코드 변경 없음, 문서만 갱신 |
| v2.3 | 2026-07-18 | **S&P 500 종목 유니버스 동기화(`sync_universe`) 추가.** 기존 일별 업데이트는 Drive에 이미 저장된 티커 목록만 갱신할 뿐 지수 편입/편출을 감지하지 못했음. 매일 업데이트 실행 전 위키피디아의 현재 구성종목과 Drive 저장 목록을 비교해, 신규 편입 종목은 5년치 백필 후 합류시키고 편출 종목은 과거 데이터는 보존하되 활성 목록(`_universe.json`)에서 제외하도록 변경. [기능 1]에 반영. 전체 내용은 [investment_assistant_spec_v2.3.md](spec_archive/investment_assistant_spec_v2.3.md) 참고 |
| v2.4 | 2026-07-18 | **[기능 4] Streamlit 대시보드 착수 (Cloud Run/Scheduler 크론 설정은 결제 계정 미연결로 보류, 수동 버튼 방식으로 대체).** ① 메인 페이지(`app.py`): "S&P 500 검색 & 데이터 적재" 버튼 하나로 `sync_universe`+`run_daily_update`를 수동 실행, 진행 로그 실시간 스트리밍. ② 종목 차트 페이지(`pages/1_종목_차트.py`): 캔들차트 + Donchian/트레일링 스탑 라인 오버레이 + 거래량/ATR 서브플롯, 기본 6개월 표시, 지표 설명, 섹터(GICS Sector, 위키피디아에서 함께 수집해 `_universe.json`에 저장)별 종목 필터, 섹터 선택은 즉시 반영되지만 종목/기간 조회는 폼으로 감싸 버튼 클릭 시에만 렌더링, HTS 스타일(테두리, 우측 세로 범례, unified hover, 등락 색상 표시 가격 헤더). [기능 4]에 반영. 전체 내용은 [investment_assistant_spec_v2.4.md](spec_archive/investment_assistant_spec_v2.4.md) 참고 |
| v2.5 | 2026-07-18 | **대표 자산군 추세추종 확장 + LLM 매매 추천 + 과거 시그널 마킹 + UI 3탭 재구성.** ① [기능1]: S&P 500 지수 자체(및 금/미국채/원자재)는 직접 거래가 불가능하므로 유동성 높은 ETF 프록시(SPY/GLD/TLT/DBC)를 S&P 500 편입/편출 로직과 분리해 별도로 수집·관리(`data_fetcher.ASSET_CLASS_TICKERS`, `run_asset_class_update`). ② [기능2]: 동일한 Donchian/ATR/트레일링 스탑 규칙이 개별 종목뿐 아니라 자산군 ETF에도 그대로 적용됨을 명시, 볼린저 밴드(20일 SMA±2표준편차, 참고용) 추가, 매수/청산 "최초 발생일"만 표시하는 Buy_Trigger/Sell_Trigger 컬럼 추가. ③ [기능3]: 뉴스 노이즈 필터링 브리핑에 더해, 뉴스+현재 시그널 상태를 함께 근거로 매수/HOLD/매도를 결론 내리는 `generate_recommendation` 추가. ④ [기능4]: 화면을 사이드바 멀티페이지 대신 탭 3개(대표 자산군 분석 / 종목 차트(S&P 500) / 데이터 적재)로 재구성. 두 차트 탭은 공통 렌더러를 공유하며, "조회" 버튼 클릭 시 차트에 과거 매수/청산 시그널 발생일을 삼각형 마커로 표시하고 LLM 매매 추천을 함께 보여줌. 데이터 적재 탭의 버튼은 S&P 500 유니버스 동기화+전종목 갱신+자산군 ETF 갱신을 한 번에 실행(`run_full_collection`). [기능1~4] 전체 갱신. 전체 내용은 [investment_assistant_spec_v2.5.md](spec_archive/investment_assistant_spec_v2.5.md) 참고 |
| v2.6 | 2026-07-19 | **탭1 콘텐츠 보강 + LLM 캐싱/투명성 강화 + 자산군 대폭 확장.** ① [기능4]: 탭1에 프로그램 목표 및 톰 바소 철학 설명을 추가하고, 지표 설명 표를 각 차트 하단(반복 노출)에서 탭1 상단(한 곳)으로 이동. ② [기능3]: `generate_recommendation` 캐시 TTL을 1시간→24시간으로 강화해 LLM 과다 호출 방지, 분석에 사용된 뉴스 기사를 카드(제목/링크/발행처·시각/요약) 형태로 함께 표시. ③ [기능1]: 뉴스 기사를 Drive에 날짜별 아카이브(`_news_{date}.json`, `news_fetcher.archive_news`)로 저장 — 기사 링크(없으면 제목) 기준 중복 적재 방지. ④ [기능1]: 대표 자산군을 4종 → 10종으로 확장 — 비트코인(`BTC-USD`), 미국 중기국채/7-10년물(`IEF`) 신규 추가, 원자재를 `DBC`(종합) 외 `USO`(원유)/`UNG`(천연가스)/`DBA`(농산물)/`CPER`(구리)로 세분화. `ASSET_CLASS_TICKERS`가 `{티커: {label, category}}` 구조로 변경되고, 탭1 UI도 탭2의 섹터→종목 패턴과 동일하게 카테고리(주식/암호화폐/귀금속/채권/원자재) → 세부 종목 2단계 선택으로 개편. [기능1·3·4] 전체 갱신. 전체 내용은 [investment_assistant_spec_v2.6.md](spec_archive/investment_assistant_spec_v2.6.md) 참고 |
| v2.7 | 2026-07-19 | **탭 구조 재편(소개 탭 신설) + 뉴스 수집을 Exa API로 전환.** ① [기능4]: 화면을 4탭(소개 / 대표 자산군 분석 / 종목 차트(S&P 500) / 데이터 적재)으로 재편. 프로그램 목표·톰 바소 철학·지표 설명을 분석 기능이 없는 별도 "소개" 탭으로 분리하고(v2.6에서 "대표 자산군 분석" 탭에 섞여있던 것을 이동), 실제 지표 분석은 2번째 탭부터 시작하도록 정리. 페이지 최상단의 앱 전체 제목(`st.title`)은 삭제(탭 안 헤더로는 유지). ② [기능3]: 뉴스 수집을 yfinance 내장 피드에서 **Exa 검색 API**(`news_fetcher.fetch_ticker_news_exa`, `category: "news"`, 최근 7일 기본)로 전환 — LLM 매매 추천(`get_recommendation`)이 이제 이 함수를 호출한다. 응답 형식은 기존과 동일(title/summary/publisher/link/published_at)하게 매핑해 뉴스 카드 표시·Drive 아카이빙 등 다운스트림 로직은 변경 없음. `publisher`는 Exa 응답의 `author`가 없으면 기사 URL 도메인으로 대체. [기능3·4] 전체 갱신. 전체 내용은 [investment_assistant_spec_v2.7.md](spec_archive/investment_assistant_spec_v2.7.md) 참고 |
| v2.8 | 2026-07-19 | **자동화 인프라(GitHub Actions + Telegram) 전체 구축, LLM 장애 내성 강화, 종목 설명 추가, 차트 한글 깨짐/기간 수정, 데드 코드 정리.** 새 [기능 5] 섹션으로 정리. 핵심: ① GCP Cloud Run/Scheduler 대신 **GitHub Actions**(`collect.yml`+`recommend.yml`, `workflow_run`으로 연쇄)로 실제 무인 자동화 완성 — 데이터 신선도 게이트(`_is_data_fresh`, 4일)로 "워크플로는 성공했지만 개별 종목만 조용히 실패"한 경우를 걸러냄. ② 매수/HOLD/매도 판정을 `signal_engine.get_mechanical_action`으로 완전히 결정론화(네트워크 호출 없음) — HOLD인 날은 뉴스/LLM 호출을 아예 생략해 API 사용량과 rate limit 위험을 크게 줄이고, 매수/매도인 날 LLM 호출이 실패해도 `_build_rule_based_explanation`으로 폴백해 판정 자체는 절대 유실되지 않도록 함(실제 429 상황에서 검증). ③ **텔레그램 알림**(`telegram_notifier.py`) 추가 — 크론에서만 발동, 전체 요약 텍스트 + 매수/매도 종목만 차트 이미지 첨부. ④ 종목별 설명 추가 — 자산군 10종은 큐레이션, S&P 500 503종목은 위키피디아에서 이미 긁던 데이터(회사명+GICS 세부업종)를 `_universe.json`에 재활용 저장, Streamlit 양쪽 탭과 텔레그램 모두에 노출. ⑤ 텔레그램 차트 한글 깨짐 수정(kaleido가 헤드리스 Chromium으로 렌더링하는데 Ubuntu 러너에 한글 폰트가 없어 발생 — `fonts-nanum` 설치 + 명시적 폰트 패밀리 지정) 및 전체 히스토리 대신 **최근 6개월**만 표시하도록 수정. ⑥ 코드 리팩토링: `chart_builder.py`로 차트 생성 로직 통합(`slice_to_period` 공용화로 Streamlit/텔레그램 기간 슬라이싱 중복 제거), 완전히 대체되어 아무도 호출하지 않던 `news_fetcher.fetch_ticker_news`(yfinance)·`fetch_news_for_tickers`·`openrouter_briefing.generate_briefings` 삭제. [기능1~5] 전체 갱신. 전체 내용은 [investment_assistant_spec_v2.8.md](spec_archive/investment_assistant_spec_v2.8.md) 참고 |
| v2.9 | 2026-07-19 | **텔레그램 일일 요약을 표 형태로 재구성.** Telegram Bot API는 `MarkdownV2`/`HTML`/레거시 `Markdown` 어느 parse_mode에서도 GFM 테이블 문법이나 HTML `<table>`을 렌더링하지 않는다 — 실질적으로 가능한 표 형태는 등폭 서체 코드블록(`` ``` ``) 안에 컬럼을 정렬하는 방법뿐이다. `telegram_notifier.format_summary`를 종목별 문장 나열(이모지+굵게 액션+종가+설명) 대신, 티커/구분(카테고리)/액션/종가 4열 표로 재작성 — 한글(매수/매도)과 영문(HOLD)이 섞여도 어긋나지 않도록 `unicodedata.east_asian_width`로 전각 문자를 2칸으로 계산해 정렬(`_display_width`/`_pad`). 이모지는 열 폭이 클라이언트마다 달라 행 안에 넣으면 정렬이 깨지므로 표 밖 범례 한 줄(🟢 매수 ⚪ HOLD 🔴 매도)로만 사용. 종목 설명은 표에서 제거(매수/매도 종목은 어차피 차트 이미지 캡션에 설명이 포함됨) — 대신 짧은 카테고리(주식/암호화폐/귀금속/채권/원자재)를 표시. 실제 텔레그램 앱에서 정렬 확인 완료. [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v2.9.md](spec_archive/investment_assistant_spec_v2.9.md) 참고 |
| v3.0 | 2026-07-19 | **자산군 10종 통합 일일 리포트(LLM 종합 해설 포함)를 GitHub Pages로 공개 발행.** ① 새 `report_builder.py`: 그날의 판정 결과로 HTML 리포트 1장을 생성 — 상단에 LLM 교차 자산 총평(`openrouter_briefing.generate_portfolio_overview`, "미스터 세레니티" 톤으로 여러 자산군에 걸친 동시 시그널만 담백하게 짚고 전망은 배제, 판정 자체는 절대 바꾸지 않는 해설 전용), 티커/자산/구분/액션/종가 요약 표, 10종목 전체의 캔들차트(Plotly `fig.to_html()`로 임베드 — 브라우저가 클라이언트에서 렌더링). ② 리포트는 Drive에 `_report_{date}.html`로도 저장되고(Streamlit "리포트 히스토리" 탭이 재계산 없이 다시 보여줌), `docs/reports/{date}.html`로도 로컬에 기록되어 `recommend.yml`이 커밋+푸시 — **GitHub Pages**(저장소 Settings에서 `master` 브랜치 `/docs` 폴더로 활성화, `https://garam827.github.io/ai_invest_assistant/`)가 이 파일을 그대로 공개 URL로 서빙한다. ③ 텔레그램은 이제 종목별 차트 이미지를 따로 첨부하지 않고, 표 요약 메시지 끝에 리포트 URL 링크 한 줄만 추가 — 하루 메시지 수가 최대 6건에서 1건으로 줄었다. ④ **kaleido/헤드리스 Chromium 렌더링 전면 제거**: 차트가 더 이상 정적 PNG로 서버 렌더링되지 않고 브라우저(Streamlit UI, GitHub Pages 리포트 페이지 모두)에서 JS로 렌더링되므로, v2.8~v2.9의 CJK 폰트 설치 문제 자체가 사라짐 — `recommend.yml`의 `fonts-nanum` 설치 스텝과 `requirements.txt`의 `kaleido` 의존성을 삭제. ⑤ Streamlit에 새 탭 **"리포트 히스토리"** 추가(날짜 선택 → 저장된 리포트를 `st.iframe`으로 그대로 표시). [기능 1·3·4·5] 전체 갱신. 전체 내용은 [investment_assistant_spec_v3.0.md](spec_archive/investment_assistant_spec_v3.0.md) 참고 |
| v3.1 | 2026-07-19 | **매수/매도 종목의 뉴스+LLM 분석을 리포트에 노출, 접이식 UI로 재구성.** `recommendation_engine.get_recommendation_for_ticker`가 매수/매도 종목에 한해 이미 Exa 뉴스를 수집하고 LLM 서술 분석을 만들고 있었는데([기능 3]), `report_builder.py`는 그 결과(`results[ticker]['news']`/`['text']`)를 전혀 쓰지 않고 버리고 있었다 — 이번 버전에서 그대로 리포트에 노출시켰다(뉴스 API를 새로 호출하지 않고 이미 계산된 값을 재사용). ① 매수/매도 종목마다 "시그널 카드"(`<section class="signal-card">`)를 만들어 액션 배지·LLM(또는 규칙 기반 폴백) 분석 텍스트·참고 뉴스 카드(제목·링크·발행처·요약)를 표시. ② 페이지 비대화 대응: 각 카드의 차트는 `<details>` 접이식 위젯 안에 넣어 기본은 접힌 상태로 시작하고, HOLD 종목(뉴스/분석 없음)은 개별 카드 대신 페이지 하단의 접힌 "HOLD 종목 차트" 블록 하나로 묶었다. ③ 요약 표의 매수/매도 티커 셀이 해당 시그널 카드로 점프하는 앵커 링크가 됨. ④ LLM 출력·Exa 기사 제목/요약처럼 외부에서 온 텍스트는 모두 `html.escape`로 이스케이프해 리포트에 삽입(꺾쇠괄호 등이 포함된 LLM 응답이 마크업을 깨뜨리지 않도록). 실제 GitHub Pages URL에서 시그널 카드 10개·뉴스 카드 25개 정상 렌더링 확인. [기능 3·5] 갱신. 전체 내용은 [investment_assistant_spec_v3.1.md](spec_archive/investment_assistant_spec_v3.1.md) 참고 |
| v3.2 | 2026-07-20 | **대표 자산군에 나스닥(`QQQ`) 추가 (10종 → 11종).** `data_fetcher.ASSET_CLASS_TICKERS`에 `QQQ`(나스닥 100 추종 ETF, 기술주 비중 높음, "주식" 카테고리 — `SPY`와 동일 카테고리)를 추가. S&P 500 지수 자체를 `SPY`로 추적하는 것과 동일한 논리로, 나스닥 지수 역시 원지수 대신 유동성 높은 ETF 프록시(`QQQ`)로 추적한다. `ASSET_CLASS_TICKERS` 딕셔너리 값 추가 외에 다른 코드 변경은 필요 없었다 — 수집(`run_asset_class_update`)·시그널·추천·리포트·텔레그램·Streamlit UI(카테고리 셀렉트박스 포함) 전부 이 딕셔너리를 기준으로 동작하도록 이미 설계돼 있었기 때문(`data_fetcher._update_one_ticker`가 신규 티커도 자동으로 5년치 백필). [기능 1] 갱신. 전체 내용은 [investment_assistant_spec_v3.2.md](spec_archive/investment_assistant_spec_v3.2.md) 참고 |
| v3.3 | 2026-07-20 | **리포트 PC 여백 개선 + Exa 뉴스 검색어를 매크로 지향으로 교체.** ① [기능 5]: `report_builder.py`의 리포트 컨테이너 `max-width`를 1000px → 1400px(`width: 94%`)로 넓히고, 뉴스 카드를 세로 나열 대신 반응형 그리드(`grid-template-columns: repeat(auto-fit, minmax(320px, 1fr))`)로 배치 — 데스크톱 화면에서 좌우 여백이 과도했던 문제와, 넓어진 공간을 그냥 비워두지 않고 카드가 자동으로 여러 열로 배치되게 함께 해결. ② [기능 3]: `ASSET_CLASS_TICKERS`의 각 자산군 항목에 `news_query`(영문, 매크로 지향 검색어) 필드를 추가하고, `recommendation_engine.get_recommendation_for_ticker`가 뉴스 수집 시 티커 대신 이 쿼리를 우선 사용하도록 변경. 문제였던 것: `GLD stock news`처럼 티커 그대로 검색하면 "GLD 몇 % 하락"류의 단순 가격 기사나 시세 페이지만 나오고, 그 등락의 배경(금리·달러·중앙은행 수요 등)을 설명하는 뉴스는 거의 안 나왔음 — `gold price outlook macro drivers analysis`처럼 프록시가 추종하는 실물 자산 이름 + 매크로 키워드로 바꾸자 Fed 금리·중앙은행 금 매입·지정학 리스크 등 실제 유용한 기사로 대체됨(로컬 비교 테스트로 확인). S&P 500 개별 종목처럼 `news_query`가 없는 경우는 기존 그대로 `"{티커} stock news"`를 쓴다(티커 자체가 회사이므로 문제 없음). [기능 3·5] 갱신. 전체 내용은 [investment_assistant_spec_v3.3.md](spec_archive/investment_assistant_spec_v3.3.md) 참고 |
| v3.4 | 2026-07-20 | **리포트 여백 추가 개선 — v3.3의 1400px도 넓은 모니터에서는 여전히 부족하다는 피드백.** `body`의 `max-width` 자체를 없애고(`max-width: none`) `width: 97%`만으로 화면 폭에 맞춰 거의 끝까지 늘어나게 변경 — 표·차트·뉴스 그리드는 이제 컨테이너 폭 제한이 없다. 다만 총평(`.overview`)·개별 분석(`.analysis`) 문단은 너무 넓으면 한 줄이 지나치게 길어져 가독성이 떨어지므로 `max-width: 900px`로 별도 제한 — "표/차트/뉴스는 화면 폭 전부, 긴 글줄은 읽기 편한 폭으로"라는 원칙으로 분리했다. [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v3.4.md](spec_archive/investment_assistant_spec_v3.4.md) 참고 |
| v3.5 | 2026-07-20 | **수동 테스트용 LLM/뉴스 스킵 토글 + 리포트 표 카테고리 그룹화 + 뉴스 카드 레이아웃 재조정.** ① `recommend.yml`의 `workflow_dispatch`에 `skip_llm_and_news`(boolean, 기본값 `true`) 입력을 추가 — 켜져 있으면 `config.SKIP_LLM_AND_NEWS`가 `true`가 되어 `recommendation_engine.get_recommendation_for_ticker`와 `report_builder`의 LLM 총평 생성이 Exa/OpenRouter 호출을 건너뛰고 곧바로 규칙 기반 설명으로 대체한다 — UI/파이프라인만 확인하는 수동 실행에서 매번 실제 API를 태우지 않도록. `workflow_run`으로 연쇄되는 실제 스케줄 실행에는 이 입력값 자체가 존재하지 않아 항상 `false`로 남는다. ② [기능 5]: 리포트 요약 표를 자산군 카테고리별로 그룹화 — `results`가 `ASSET_CLASS_TICKERS` 정의 순서를 그대로 따르므로(같은 카테고리가 이미 연속으로 붙어 있음) 카테고리가 바뀌는 지점마다 `colspan` 그룹 헤더 행만 끼워 넣어 별도 정렬 로직 없이 구현, 반복되던 "구분" 컬럼은 그룹 헤더로 대체되어 표에서 제거. ③ 뉴스 카드를 반응형 그리드(여러 열) 대신 한 열로 되돌리고 폭을 넓게(가로로 길게) 쓰도록 변경, 카드 폰트도 살짝 축소(`0.88rem`, 메타 정보는 `0.8rem`). [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v3.5.md](spec_archive/investment_assistant_spec_v3.5.md) 참고 |
| v3.6 | 2026-07-20 | **뉴스 카드 레이아웃 확정 + 텔레그램 그룹 채팅 전환(운영) + 문서 정합성 점검.** ① `report_builder.py`의 `.news-card`에 `width: 100%`를 명시(기존 `flex-direction: column`만으로도 한 줄에 카드 하나만 나올 수밖에 없는 구조였지만, 오해를 방지하기 위해 명시적으로 고정 — 동작 변화 없음, 실제 뉴스가 채워진 라이브 리포트로 재검증). ② **운영 변경**: 텔레그램 알림을 1:1 봇 채팅에서 그룹 채팅으로 전환 — 로컬 `.env`의 `TELEGRAM_CHAT_ID`만 바꾸고 GitHub Secret은 갱신 안 되어 있던 것을 발견해 `gh secret set`으로 동기화(로컬 `.env`와 GitHub Actions Secrets는 완전히 별개 저장소라는 게 이번에 실제로 드러난 함정 — 주의사항 12번에 명문화), `skip_llm_and_news=true`로 API 비용 없이 새 그룹 수신 확인. ③ `TELEGRAM_SETUP.md` 전면 갱신: v3.0 이후 사라진 `send_photo`/차트 이미지 첨부 관련 서술 제거, "1:1 → 그룹 전환" 절차와 `skip_llm_and_news` 테스트 방법 섹션 신설, ".env와 Secret 따로 논다" 트러블슈팅 항목 추가. ④ 주의사항 12·13번 신설(`.env`/Secret 별도 관리, `skip_llm_and_news` 활용법). 코드 변경은 ①의 CSS 한 줄뿐이고 나머지는 운영/문서 정리. [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v3.6.md](spec_archive/investment_assistant_spec_v3.6.md) 참고 |
| v3.7 | 2026-07-21 | **크론 스케줄 요일 버그 수정 + 프로젝트 루트 정리.** ① `collect.yml`의 스케줄 요일 마스크가 UTC 기준 월~금(`1-5`)이었는데, 한국시간(UTC+9)으로 환산하면 자정을 넘기면서 하루씩 밀려 실제로는 **화~토 아침**에 알림이 가고 있었다(월요일 아침 알림 누락 + 불필요한 토요일 알림). 실제 스케줄 실행 기록(UTC 월 23:31 실행분이 KST 화 08:31로 찍힘)으로 재현 확인 후, `cron: "30 22 * * 0-4"`(UTC 일~목)로 수정 — UTC 일요일 실행은 그 시점엔 새 미국 거래일 데이터가 아직 없어 지난 금요일 종가를 다시 확인하는 셈인데, 이게 자연스럽게 "월요일 아침 캐치업" 역할을 하게 된다. ② 프로젝트 루트에 쌓여있던 과거 버전 스펙 문서 17개(`investment_assistant_spec_v1.md` ~ `v3.6.md`)를 `spec_archive/` 하위 디렉터리로 이동(코드 파일과 뒤섞여 난잡했던 문제 해결, `git mv`로 히스토리 보존), 현재 스펙의 모든 버전 이력 백링크를 새 경로로 갱신. `paper_trading_spec.md`(아직 미구현, 활성 설계 문서)와 `GOOGLE_DRIVE_SETUP.md`/`TELEGRAM_SETUP.md`(설정 가이드)는 자주 참조되는 문서라 루트에 유지. gitignore된 임시 파일(`full_ingestion.log`, `.cache_ggshield`)도 함께 정리. [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v3.7.md](spec_archive/investment_assistant_spec_v3.7.md) 참고 |
| v3.8 | 2026-07-21 | **크론 요일 마스크를 v3.7 이전(UTC 월~금)으로 원복 + GitHub Actions 지연 특성 문서화.** v3.7에서 "한국시간 월요일 아침 알림이 없다"는 이유로 UTC 일~목(`0-4`)로 바꿨었는데, 재검토 결과 한국시간 월요일 아침 시점엔 미국 시장이 아직 그 주의 거래를 시작하지 않아(주말 지난 금요일 종가 그대로) 알림을 보내도 중복 정보라는 결론에 도달 — `cron: "30 22 * * 1-5"`(UTC 월~금, 한국시간 화~토 07:30)로 되돌렸다. 별개로, "왜 07:30이 아니라 08:30에 실행됐냐"는 질문에서 GitHub Actions 스케줄이 예정 시각(22:30 UTC)보다 약 61분 늦게(23:31 UTC) 실행된 실제 사례를 발견 — 크론 표현식 문제가 아니라 GitHub Actions 스케줄 자체가 "best-effort"라 사용량이 몰리는 정각/반각 근처에는 지연될 수 있다는, 공식적으로 알려진 플랫폼 특성. `collect.yml` 주석과 [기능 5]에 명문화. [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v3.8.md](spec_archive/investment_assistant_spec_v3.8.md) 참고 |
| v3.9 | 2026-07-21 | **크론 시각을 06:30 KST 근처로 앞당기고 정각/반각을 피하도록 조정.** `cron: "30 22 * * 1-5"`(UTC 22:30, 한국시간 07:30경) → `"37 21 * * 1-5"`(UTC 21:37, 한국시간 06:37경)로 변경 — 기상 시간에 맞춰 앞당기면서, GitHub Actions가 `:00`/`:15`/`:30`/`:45` 같은 정각/반각 근처에 스케줄 실행을 더 많이 큐잉해 지연이 잦다는 특성을 감안해 분(分)을 `:37`처럼 의도적으로 어긋나게 잡았다. 트레이드오프: 앞당긴 만큼 EST(미국 겨울시간) 구간의 장마감 후 버퍼가 기존 1.5시간에서 약 37분으로 줄었다 — 그 시점에 yfinance 데이터가 아직 안 올라와 있으면 그날치 수집만 못 하고 다음 실행에서 자가 치유되는 수준이라 감수. [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v3.9.md](spec_archive/investment_assistant_spec_v3.9.md) 참고 |
| v3.10 | 2026-07-21 | **크론 분(分)을 `:37` → `:57`로 재조정(사용자 직접 수정).** 겨울철(EST) 버퍼를 조금 더 확보하고 싶어 `cron: "37 21 * * 1-5"`에서 `"57 21 * * 1-5"`로 20분 늦췄다 — 한국시간 도착 시각은 06:37경 → 06:57경으로, EST 버퍼는 약 37분 → 약 57분으로 늘었다. 정각/반각을 피한다는 원칙은 그대로 유지(`:57`도 비정형 분). [기능 5] 갱신. 전체 내용은 [investment_assistant_spec_v3.10.md](spec_archive/investment_assistant_spec_v3.10.md) 참고 |
| v3.11 | 2026-07-21 | **Streamlit 공개 배포 준비: LLM 비활성화 옵션 + 뉴스 버튼 트리거 + 뉴스 읽기 캐시.** 공개 배포(예: Streamlit Community Cloud) 시 API 비용/오남용을 관리하기 위한 3가지 변경. ① `config.STREAMLIT_ENABLE_LLM`(기본 `true`, 로컬 개발 영향 없음 — 배포본에서 `false`로 설정) 신설. `recommendation_engine.get_recommendation_for_ticker`에 `use_llm` 파라미터를 추가해, `False`면 뉴스는 그대로 수집하되 OpenRouter 호출은 생략하고 `_build_rule_based_explanation`으로 대체(기존 "LLM 분석 실패" 문구와 구분되게 "LLM 분석 비활성화" 문구 사용 — 실패와 의도적 비활성화를 구분). ② `app.py`: `STREAMLIT_ENABLE_LLM`이 꺼져 있을 때만 "Mr. Serenity의 매매 추천" 섹션이 자동 표시되지 않고, "뉴스/분석 불러오기" 버튼을 눌러야 실행되도록 변경(티커별 `st.session_state` 키로 게이팅해 종목을 바꾸면 자동으로 다시 잠김). 로컬 개발 기본값(`true`)에서는 기존과 동일하게 자동 표시. ③ `news_fetcher.get_cached_news(drive_db, ticker, date=None)` 신설 — 이미 있는 `_news_{date}.json` 아카이브를 읽기 캐시로 활용해, 같은 종목/날짜를 여러 사용자(또는 재실행)가 요청해도 Exa를 한 번만 호출. 크론 경로에도 동일하게 적용되어 배포 여부와 무관하게 Exa 사용량이 준다. 로컬에서 use_llm=False + 캐시 히트/미스 + 규칙 기반 폴백 문구를 실제 데이터로 검증, AppTest로 버튼 게이팅과 미설정 시 자동 표시(회귀 없음)를 각각 확인. [기능 3·4·5] 갱신. 전체 내용은 [investment_assistant_spec_v3.11.md](spec_archive/investment_assistant_spec_v3.11.md) 참고 |
| v3.12 | 2026-07-21 | **차트 서브플롯 비율 조정 + 범례 약어화 + 리포트 차트 테두리.** `chart_builder.py`(Streamlit·리포트 공용): ① 캔들:거래량:ATR 서브플롯 높이 비율을 3:1:1(`row_heights=[0.6,0.2,0.2]`)에서 **2:1:1**(`[0.5,0.25,0.25]`)로 조정 — 거래량/ATR 서브플롯이 상대적으로 더 커짐. ② 범례 트레이스 이름을 축약(`"볼린저 상단"`→`"BB상단"`, `"Donchian 상단(20일)"`→`"DC20상단"`, `"트레일링 스탑"`→`"손절선"`, `"매수 시그널"`/`"청산 시그널"`→`"매수"`/`"매도"` 등) — 짧아진 만큼 범례 오른쪽 여백(`margin.r`)도 160px→110px로 축소. `report_builder.py`: 약어만으로는 뜻이 명확하지 않을 수 있어 리포트 페이지 우상단(제목 옆, `.chart-legend-key`)에 "BB=볼린저밴드 · DC20/DC100=Donchian채널(20일/100일) · 손절선=트레일링 스탑(고점−3×ATR)" 한 줄 설명을 작게 추가. ③ 리포트에 임베드되는 모든 차트(시그널 카드 안, HOLD 접이식 블록 모두)를 `.chart-frame`(테두리+패딩) 안에 넣어 시각적으로 구분 — Streamlit 쪽은 기존에 이미 `st.container(border=True)`로 테두리가 있어 변경 없음. 실제 Plotly 도형의 y축 domain 비율(0.4:0.2:0.2)로 2:1:1 적용을 확인. [기능 2·4·5] 갱신. 전체 내용은 [investment_assistant_spec_v3.12.md](spec_archive/investment_assistant_spec_v3.12.md) 참고 |
| v3.13 | 2026-07-21 | **v3.12 미세 조정 — 서브플롯 비율 원복 + 범례 안내문 줄바꿈 + 리포트 폰트 축소.** ① `chart_builder.py`의 캔들:거래량:ATR 비율을 2:1:1에서 **3:1:1**(`row_heights=[0.6,0.2,0.2]`)로 원복(범례 약어화·오른쪽 여백 축소는 유지). ② `report_builder.py`의 우상단 범례 안내문("BB=..." 등)을 한 줄 나열 대신 `<br>`로 3줄로 나눠 표시(`text-align:right`는 유지되어 각 줄이 우측 정렬됨). ③ 리포트 전체 기본 폰트를 살짝 축소(`body font-size: 0.92rem`)하고, 요약 표는 그보다 더 작게(`table.summary font-size: 0.8rem`) — 정보 밀도가 높은 표가 상대적으로 더 조밀해 보이도록. [기능 2·5] 갱신. 전체 내용은 [investment_assistant_spec_v3.13.md](spec_archive/investment_assistant_spec_v3.13.md) 참고 |
| v3.14 | 2026-07-21 | **Streamlit 대시보드를 GCP Cloud Run에 배포하기 위한 가이드+컨테이너 준비.** 앱 배포처를 Streamlit Community Cloud가 아니라 **GCP Cloud Run**으로 결정. ① 저장소 루트에 `Dockerfile`(Python 3.11-slim, `streamlit run app.py --server.port=${PORT:-8080} --server.address=0.0.0.0` — kaleido를 더 이상 안 써서 폰트/브라우저 설치가 전혀 필요 없는 순수 Python 이미지)과 `.dockerignore` 신설. ② `GCP_STREAMLIT_DEPLOY.md` 신설 — 프로젝트/API 활성화, Drive OAuth 토큰을 헤드리스 배포용으로 미리 준비하는 절차([GOOGLE_DRIVE_SETUP.md](GOOGLE_DRIVE_SETUP.md)와 동일한 `_bootstrap_secret_file` 메커니즘 재사용), Secret Manager 등록, `gcloud run deploy`(리전 `asia-northeast3`, `--min-instances=0`, `STREAMLIT_ENABLE_LLM=false` 명시 — v3.11에서 준비해 둔 배포 모드를 실제로 사용), 비용 관리·트러블슈팅까지 포함. `.env`/GitHub Secrets에 이어 **GCP Secret Manager가 세 번째 별도 저장소**임을 다시 한번 강조(주의사항 12번과 동일 원칙). [기능 4·5] 갱신. 전체 내용은 [investment_assistant_spec_v3.14.md](spec_archive/investment_assistant_spec_v3.14.md) 참고 |
| v3.15 | 2026-07-21 | **실제 채택된 GCP 배포 경로를 Compute Engine + Docker Compose로 확정, 기존 `GCP_Setyo_Guide.md`를 이 저장소 기준으로 수정.** 사용자가 이미 트라이얼 앤 에러로 검증해 둔 별도 문서 `GCP_Setyo_Guide.md`(VM `e2-micro` + Docker + Docker Compose, 부팅 시 시작 스크립트가 자동으로 클론·빌드·기동)를 확인한 결과 저장소 이름이 `streamlit-telegram-app`(관련 없는 다른 프로젝트)으로 잘못 박혀 있었음 — `ai_invest_assistant`로 전부 수정. 그 과정에서 발견한 진짜 버그도 함께 고침: ① 문서 3-②에서 안내한 Secret 이름(`streamlit-telegram-app-github-token`)과 시작 스크립트가 실제로 조회하던 이름(`git-token`)이 서로 달라 그대로는 스크립트가 실패했을 것 — `ai-invest-assistant-github-token`으로 통일. ② 시작 스크립트가 GitHub 저장소만 클론할 뿐 앱 자체 시크릿(Drive/OpenRouter/Exa 등)을 VM에 공급하는 단계가 아예 없어서, 그대로 실행하면 `docker compose up`은 되지만 앱이 필수 환경변수 없이 즉시 죽었을 것 — Secret Manager에 앱 시크릿을 추가로 등록하는 절차와, 시작 스크립트에 `.env` 생성 + `client_secret.json`/`token.json` 다운로드 단계를 추가. ③ 저장소에 없던 `docker-compose.yml`을 신설(포트 8501, `.env` 참조, Drive OAuth JSON 2개는 이미지에 굽지 않고 런타임에 읽기 전용으로 볼륨 마운트 — `.dockerignore`가 그 파일들을 이미지 빌드 컨텍스트에서 제외하고 있어 마운트가 필수). ④ `Dockerfile`의 기본 포트를 Cloud Run 관례(8080)에서 Streamlit 자체 관례(8501)로 변경 — VM+Compose 경로가 실제 채택안이 됐으므로. `GCP_STREAMLIT_DEPLOY.md`(Cloud Run)는 미검증 대안책으로 문서 맨 위에 상태 표시만 남기고 그대로 보존(트래픽이 늘어 서버리스가 필요해지면 재검토). [기능 4·5] 갱신 |
| v3.16 | 2026-07-22 | **일목균형표(Ichimoku Kinko Hyo) 참고 오버레이 + 매매 추천 코멘트에 "구름 확인" 어드바이저리 노트 추가.** 볼린저 밴드와 동일한 원칙 — 차트에만 참고용으로 표시하고 `signal_engine.get_mechanical_action`(매수/HOLD/매도 판정)이나 포지션 사이징 공식에는 절대 관여하지 않는다. ① `signal_engine.calculate_ichimoku`: 전환선(9일)/기준선(26일)/구름(선행스팬A·B, 26일 선행 투영)/후행스팬(26일 후행 종가)을 계산해 `compute_signals`의 컬럼으로 추가(`Ichimoku_Tenkan`/`Kijun`/`SenkouA`/`SenkouB`/`Chikou`, 차트의 미래 구름 투영용 비-변위 원본값 `SenkouA_Raw`/`SenkouB_Raw`도 별도 보관). ② `signal_engine.get_ichimoku_confluence(summary, action)`: 오늘 종가가 구름 위/아래/안 중 어디 있는지, 구름이 양운(상승)/음운(하락)인지를 보고 기계적 매수/매도 판정과 방향이 일치하는지(`agrees_with_action`)만 계산하는 순수 함수 — 이 결과는 절대 `action`이나 `suggested_shares`를 바꾸지 않고, `recommendation_engine`의 서술 텍스트에 참고 문장 한 줄("추세와 일치합니다" 또는 "비중을 줄여 진입하는 것도 고려해볼 만합니다")로만 반영된다(규칙 기반 폴백·LLM 프롬프트 양쪽 모두, LLM 시스템 프롬프트에도 "이 정보로 첫 줄의 추천 자체를 바꾸지 말 것"을 명시). ③ `chart_builder.py`: 전환선/기준선/후행스팬은 기존 오버레이와 동일하게 표시하고, 구름(선행스팬A·B)은 전통적인 일목균형표처럼 마지막 캔들 이후 26영업일치 미래까지 이어지는 모습으로 그린다 — `slice_to_period`가 다루지 않는 미래 날짜축 확장이 필요해, 표시 구간의 마지막 `displacement`(26)개 원본(비변위) 값을 향후 26영업일(`pd.bdate_range`)에 이어붙여 하나의 연속된 트레이스로 구성했다(과거 구간은 이미 변위된 컬럼을 그대로 사용 — 이어붙는 지점에서 값이 자연스럽게 연속됨을 인덱스 산술로 확인). ④ 소개 탭 지표 설명 표와 리포트 우상단 범례 안내 박스(`.chart-legend-key`)에 새 줄 추가. 로컬에서 실제 SPY 데이터로 구름/전환선/기준선 값과 confluence 판정(일치/불일치 양쪽 케이스), 차트 트레이스가 미래 26일까지 끊김 없이 이어지는지, 규칙 기반 설명 문구에 어드바이저리 문장이 올바르게 붙는지 모두 확인. [기능 2·3·4] 갱신 |
| v3.17 | 2026-07-22 | **차트 서브플롯 비율을 3:1:1 → 4:1:1로 조정.** `chart_builder.py`의 캔들:거래량:ATR `row_heights`를 `[0.6, 0.2, 0.2]`에서 `[2/3, 1/6, 1/6]`로 변경 — 캔들 영역이 상대적으로 더 커짐. 실제 Plotly 도형의 y축 domain 비율(0.533:0.133:0.133)로 4:1:1 적용을 확인. [기능 2·4] 갱신 |
| v3.18 | 2026-07-22 | **차트 범례를 오른쪽 세로 고정폭 → 하단 가로 배치로 변경 + 표시 항목 축소.** 오른쪽에 고정폭(`margin.r`)으로 붙는 세로 범례가 모바일처럼 폭이 좁은 화면에서 차트 자체를 지나치게 좁혀 보이게 하는 문제 — `legend=dict(orientation="h", ...)`로 차트 하단에 배치해, 좁은 화면에서는 범례가 차트 폭을 잠식하는 대신 여러 줄로 줄바꿈되어 세로 공간만 더 쓰도록 바꿨다(`margin.r`을 120→40으로 줄이고 `margin.b`를 20→90, `height`를 950→1020으로 조정). 동시에 범례에 표시되는 항목도 축소 — 캔들(가격)·거래량·ATR은 이미 자기 (서브)플롯 자체로 뜻이 명확하므로 `showlegend=False`, BB/DC20/DC100은 각각 상단·하단 두 트레이스였던 걸 `legendgroup`으로 묶어 대표 트레이스 하나(`"BB"`/`"DC20"`/`"DC100"`)만 범례에 노출하고 나머지는 `showlegend=False`(`legend.groupclick="togglegroup"`이라 범례에서 하나를 클릭하면 그룹 전체가 함께 토글됨). 손절선과 일목균형표 5종(전환선/기준선/선행스팬A·B/후행스팬), 매수/매도 마커는 그대로 개별 노출 — 결과적으로 범례 항목이 18개에서 11개(BB·DC20·DC100·손절선·일목균형표 5종·매수·매도)로 줄었다. 로컬에서 실제 SPY 데이터로 범례 항목 수·그룹핑·수평 배치를 모두 확인. [기능 2·4] 갱신 |
| v3.19 | 2026-07-22 | **볼린저 밴드를 더 연하게 + 일목균형표는 구름(양운/음운)만 표시.** ① `chart_builder.py`의 BB 라인/채움 투명도를 낮췄다(라인 `rgba(...,0.6)`→`0.3`, 채움 `0.12`→`0.05`, 중심선 `0.4`). ② 일목균형표에서 전환선/기준선/후행스팬 3개 라인은 차트에서 제거하고(내부적으로는 여전히 계산됨 — 선행스팬A가 전환선/기준선으로부터 계산되므로), 구름(선행스팬A·B)만 남겨 **양운(선행스팬A≥B, 연한 붉은색)/음운(연한 파란색)**으로 구분해 표시한다. Plotly는 트레이스 하나의 채움 색을 조건부로 바꿀 수 없어, 선행스팬A−B의 부호가 바뀌는 지점마다 구간을 나눠 구간별로 별도의 채움 트레이스(양운/음운 각각 한 쌍)를 그리는 방식으로 구현했다 — 인접 구간이 시각적으로 이어지도록 각 구간은 다음 구간의 첫 지점을 함께 포함한다. 범례에는 양운/음운 각각 처음 등장한 구간에서만 한 번씩 노출(`legendgroup`+`showlegend`로 중복 제거). 소개 탭 지표 설명과 리포트 범례 안내 박스도 이에 맞춰 갱신. 로컬에서 실제 SPY 데이터로 구간 분할·범례 노출 여부·색상(rgba 값)을 모두 확인. [기능 2·4] 갱신 |
| v3.20 | 2026-07-23 | **대표 자산군에 달러 인덱스(`UUP`) 추가 (11종 → 12종) + 샘플 리포트에 `_test` 접미어 도입.** ① `data_fetcher.ASSET_CLASS_TICKERS`에 새 카테고리 **"통화"**로 `UUP`(Invesco DB US Dollar Index Bullish Fund — 주요 6개국 통화 대비 미국 달러 강세 추종 ETF)를 추가. 다른 자산군과 완전히 동일한 취급 — `signal_engine`은 ticker-agnostic이라 코드 변경 없이 동일한 Donchian/ATR/일목균형표 규칙이 그대로 적용되고(v3.2에서 QQQ 추가 때와 같은 패턴), `run_asset_class_update`/추천/리포트/텔레그램/Streamlit UI(카테고리 셀렉트박스 포함)도 전부 `ASSET_CLASS_TICKERS` 딕셔너리 기준으로 동작해 자동으로 포함된다. `data_fetcher.run_initial_ingestion(drive_db, tickers=["UUP"])`로 5년치(`config.INITIAL_HISTORY_PERIOD`) 히스토리를 한 번 백필해 Drive에 저장(1255행, 다른 자산군과 동일 백필 경로). ② **`config.IS_TEST_REPORT`** 신설(`recommend.yml`의 새 `workflow_dispatch` 입력 `is_test`, 기본값 `true` — `skip_llm_and_news`와 동일한 패턴) — 켜져 있으면 `recommendation_engine.run_asset_class_recommendations`가 리포트 파일명(Drive `_report_{date}.html`, 로컬 `docs/reports/{date}.html`, 텔레그램에 링크되는 `report_url`)에만 `_test`를 붙여 `{date}_test.html`로 저장한다 — `_recommendations_{date}.json`(추천 원본 데이터)은 영향받지 않음, 실제 스케줄 실행(`workflow_run`)에는 이 입력 자체가 없어 항상 실제 파일명을 쓴다. 수동으로 샘플/테스트 리포트를 발행할 때 그날의 실제 리포트를 덮어쓰지 않기 위한 안전장치 — 커밋 메시지도 `Add test report ...`/`Add daily report ...`로 구분된다. [기능 1·5] 갱신 |
| v3.21 | 2026-07-24 | **모의 투자(Paper Trading) 기능 구현 — `paper_trading_spec.md`(설계 대기 문서)를 실제로 구현.** 새 `[기능 6]` 섹션 신설, 이 문서에 통합하며 `paper_trading_spec.md`는 `spec_archive/paper_trading_spec_v1.md`로 이동(다른 기능들과 동일한 버전 보존 방식). ① 새 모듈 `paper_trading.py`: `_paper_trades.json`(Drive, `DriveDB.load_json`/`save_json` 재사용)에 포지션을 저장. `open_position`은 체결가 미지정 시 매수일 종가를 자동 조회(주말/휴일 선택 시 그 이전 최근 거래일 종가로 대체), `close_position`은 청산가 미지정 시 최신 종가를 자동 조회. `compute_position_returns`가 보유 중 포지션엔 `current_price`/`unrealized_pnl`/`unrealized_pnl_pct`를, 청산된 포지션엔 `realized_pnl`/`realized_pnl_pct`를 계산해 붙인다 — 이 계산은 이미 Drive에 있는 시세만 재사용하므로 yfinance/Exa/OpenRouter 등 새로운 외부 API 호출이 전혀 없다. ② `app.py`에 6번째 탭 **"모의 투자"** 신설(스펙의 핵심 설계 결정대로 포지션 추가/청산은 이 UI에서만 가능 — 크론은 읽기 전용). 종목 유형(대표 자산군/S&P 500 개별종목)을 라디오로 고른 뒤 각각 기존 탭 2/3와 동일한 카테고리→종목, 섹터→종목 선택 패턴을 재사용. 보유 포지션은 카드형(`st.container(border=True)`)으로 나열하고 포지션별 청산 폼을 붙였으며, 손익은 초록/빨강 색상(`:green[...]`/`:red[...]`)으로 표시. 청산 내역은 접힌 expander에 표로만 표시(조작 불가). ③ `report_builder.build_daily_report_html`에 `paper_positions` 인자 추가 — 보유 중인 포지션이 있으면 요약 표 바로 아래에 "모의 투자 현황" 섹션(티커/매수일·가/수량/현재가/미실현손익%, 손익 색상 표시)을 추가하고, 없으면 섹션 자체를 생략한다(HOLD 종목에 뉴스/분석이 없는 것과 동일한 원칙). ④ `telegram_notifier.format_summary`에도 `paper_positions` 인자 추가 — 보유 포지션이 있을 때만 기존 액션 요약 표 아래에 같은 등폭 코드블록 형식으로 포지션 표를 추가(기존 표 정렬 헬퍼 `_display_width`/`_pad`를 `_format_table`로 추출해 재사용, 중복 없음). ⑤ `recommendation_engine.run_asset_class_recommendations`가 리포트 생성 직전에 `paper_trading.load_positions`+`compute_position_returns`를 호출해 위 두 곳에 전달 — 이 조회 자체도 try/except로 감싸 실패해도 리포트/텔레그램/추천 결과 저장 중 어느 것도 막지 않는다(기존 "이미 성공한 걸 절대 놓치지 않는다" 원칙과 동일). 실제 SPY 포지션을 열고(`open_position`) Streamlit AppTest로 탭 렌더링·청산 버튼 클릭까지 실제 위젯 상호작용으로 검증, `report_builder`/`telegram_notifier` 로컬 출력과 실제 `recommend.yml` 테스트 발행(`is_test=true`)으로 라이브 리포트에 섹션이 정상 노출되는 것까지 확인. [기능 4·5·6] 갱신 |
| v3.22 | 2026-07-24 | **`_test` 접미어를 리포트뿐 아니라 추천 결과 JSON에도 적용.** v3.20에서 도입한 `config.IS_TEST_REPORT`는 리포트 파일명(`_report_{date}.html`/`docs/reports/{date}.html`)에만 `_test`를 붙이고 `_recommendations_{date}.json`은 항상 그냥 날짜를 썼는데, 이 때문에 수동 테스트 발행이 그날의 **실제 추천 결과 아카이브**는 그대로 덮어쓰는 구멍이 남아 있었다(로컬에서 재현 확인: `IS_TEST_REPORT=true`로 실행 시 `_recommendations_2026-07-24.json`을 다시 덮어쓸 뻔함). `recommendation_engine.run_asset_class_recommendations`에서 `_test` 접미어 계산을 추천 JSON 저장 이전으로 끌어올려, 하나의 `file_date` 변수를 추천 JSON과 리포트 양쪽에 동일하게 적용하도록 수정 — `_recommendations_{date}_test.json`이 실제로 별도 생성되고 그날의 진짜 파일은 그대로 남는 것을 Drive에서 직접 확인. [기능 5] 갱신 |
| v3.23 | 2026-07-24 | **모의 투자 포지션에 "설정일"(포지션을 실제로 기록한 날짜) 노출.** `paper_trading.open_position`이 처음부터 저장해 두던 `created_at`(포지션 생성 타임스탬프)이 지금까지 어디에도 표시되지 않고 있었다 — 예를 들어 "SPY를 7월 20일 종가에 매수했다"는 포지션을 실제로는 7월 24일에 입력했을 수 있는데(`entry_date`는 소급 지정 가능), 이 둘을 구분할 방법이 없었다. 스키마 변경 없이(이미 저장돼 있던 값을 노출만 함) `report_builder._build_paper_trading_html`의 "모의 투자 현황" 표에 `created_at`의 날짜 부분(`[:10]`)을 "설정일" 컬럼으로 추가했고, `app.py`의 "모의 투자" 탭(보유 포지션 카드 + 청산 내역 표)에도 동일하게 추가했다. 실제로 매수일 7/20·설정일 7/24로 다른 포지션을 만들어 리포트 HTML과 Streamlit 탭(AppTest) 양쪽에서 두 날짜가 서로 다르게 정확히 표시되는 것을 확인. [기능 6] 갱신 |
| v3.24 | 2026-07-25 | **"리포트 히스토리" 탭의 iframe 임베드를 새 탭 링크로 교체.** v3.0부터 `st.iframe(report_html, height=9500)`으로 리포트를 그대로 임베드해 왔는데, 리포트 자체가 v3.4에서 `max-width` 제한을 없애고 화면 폭 전체를 쓰도록 설계돼 있어 Streamlit 탭의 좁은 컨테이너 안 iframe에 넣으면 그 좁은 폭 기준으로 다시 눌리고, 바깥 페이지 스크롤과 iframe 내부 스크롤이 겹쳐 가독성이 나빠지는 문제가 있었다. `st.link_button`으로 GitHub Pages에 이미 발행된 리포트 URL(`{REPORT_BASE_URL}/{date}.html`)을 새 탭에서 열도록 바꿔, 리포트가 원래 설계된 전체 폭·단일 스크롤로 보이게 했다. Drive에는 저장됐지만 `recommend.yml`의 뒤이은 git 커밋+푸시 단계가 실패해 GitHub Pages에 아직 반영되지 않은 극히 드문 경우를 대비해, Drive에서 읽은 HTML을 그대로 받을 수 있는 `st.download_button`을 보조로 함께 두었다. Streamlit AppTest로 예외 없이 렌더링되는 것과 `download_button` 정상 노출을 확인(`link_button`은 AppTest에 타입 지원이 없어 예외 미발생으로만 간접 확인). [기능 4] 갱신 |
| v3.25 | 2026-07-25 | **리포트 히스토리 날짜 선택을 드롭다운 → 세로 목록으로, `_test` 리포트는 목록에서 제외.** ① `app.py`의 날짜 선택 위젯을 `st.selectbox`에서 `st.radio`로 변경 — 드롭다운을 열지 않아도 저장된 날짜를 한눈에 훑어볼 수 있다. ② `report_builder.list_report_dates`가 `_test` 접미어(`config.IS_TEST_REPORT`, v3.20)가 붙은 날짜를 반환 목록에서 제외하도록 수정 — 수동 샘플 발행이 실제 리포트 이력 목록에 섞여 나오지 않는다. 필터링을 `list_report_dates` 안에서 하므로 이 함수를 쓰는 모든 곳에 자동 적용된다. 실제 Drive 데이터로 `_test` 날짜가 목록에서 빠지는 것과 Streamlit AppTest로 라디오 목록이 정상 렌더링되는 것을 확인. [기능 4] 갱신 |
| v3.26 | 2026-07-25 | **리포트 히스토리를 "날짜 선택 → 리포트 하나 보기"에서 "날짜별 한 줄 표"로 재구성.** v3.25의 라디오 목록(날짜 하나만 골라 링크/다운로드)을 없애고, 저장된 각 날짜를 `st.container(border=True)` 카드 한 줄로 나열해 날짜 + `ASSET_CLASS_TICKERS` 12종 각각의 그날 액션(매수=`B`/HOLD=`H`/매도=`S`, 기존 액션 색상 팔레트로 색상 표시, 데이터 없는 티커는 `-`) + 링크(`st.link_button`, GitHub Pages) + 다운로드(`st.download_button`, Drive HTML)를 한 번에 보여준다 — 날짜를 먼저 골라야 리포트 내용을 짐작할 수 있던 이전 방식과 달리, 표만 훑어봐도 그날 어떤 자산군이 움직였는지 바로 파악할 수 있다. 액션 컬럼을 채우기 위해 `recommendation_engine.load_recommendations(drive_db, date)`(신설 — 기존엔 `_recommendations_{date}.json` 저장 함수만 있고 대칭되는 조회 함수가 없었음)로 그날의 `_recommendations_{date}.json`을 읽어온다. `_test` 리포트는 v3.25와 동일하게 `report_builder.list_report_dates`에서 이미 제외되어 표에 나타나지 않는다. 실제 Drive 데이터로 12개 티커 컬럼과 색상, UUP 추가 이전 날짜의 `-` 표시, 링크/다운로드 버튼까지 Streamlit AppTest로 확인. [기능 4] 갱신 |
| v3.27 | 2026-07-25 | **`_test` 리포트가 실제로는 여전히 `docs/reports/`에 커밋 → GitHub Pages에 공개되고 있던 근본 원인 수정.** v3.25에서 `report_builder.list_report_dates`가 `_test` 날짜를 Streamlit 목록에서만 걸러냈을 뿐, `recommendation_engine.run_asset_class_recommendations`는 여전히 테스트 발행마다 `docs/reports/{date}_test.html`을 로컬에 써서 `recommend.yml`이 그대로 git에 커밋+푸시하고 있었다 — UI에서만 안 보였지 실제로는 저장소와 GitHub Pages에 계속 쌓이고 있던 것(`docs/reports/2026-07-24_test.html`이 실제로 커밋되어 남아있던 것을 확인, `git rm`으로 제거). `report_builder.save_report`가 `date`가 `_test`로 끝나면 Drive 저장 이후 로컬 `docs/reports/` 파일 쓰기 자체를 건너뛰도록 수정 — 이제 테스트 발행은 Drive에만 남고 `docs/reports`/git/GitHub Pages 어디에도 닿지 않는다. 이에 맞춰 `recommendation_engine.run_asset_class_recommendations`도 `config.IS_TEST_REPORT`일 때는 `report_url`을 아예 만들지 않도록 수정(어차피 게시되지 않아 404가 될 URL을 텔레그램에 보내지 않기 위함). 로컬에서 `_test` 날짜로 `save_report`를 직접 호출해 Drive에는 저장되지만 `docs/reports/`에는 아무 파일도 생기지 않는 것을 확인. [기능 4·5] 갱신 |
| v3.28 | 2026-07-25 | **리포트 히스토리 표의 액션 컬럼용 경량 누적 파일 `_signal_history.json` 신설.** v3.26에서 표를 그릴 때 날짜마다 `_recommendations_{date}.json`(LLM 서술 텍스트·뉴스까지 포함된 무거운 파일)을 하나씩 따로 읽고 있었는데, 정작 필요한 건 티커별 액션 문자열뿐이었다 — 날짜가 쌓일수록 Drive 읽기 횟수와 불필요한 데이터 전송이 비례해서 늘어나는 구조. `recommendation_engine.py`에 `{날짜: {티커: 액션}}` 구조의 작은 누적 파일(`SIGNAL_HISTORY_FILENAME = "_signal_history.json"`, 기존 `DriveDB.load_json`/`save_json` 재사용)을 신설하고, `run_asset_class_recommendations`가 매일 **실제(비-테스트) 발행**마다 그날의 액션만 이 파일에 갱신하도록 연결했다(`config.IS_TEST_REPORT`일 때는 건너뛰어, 테스트 발행이 영구 이력에 섞이지 않게 함 — v3.27과 같은 원칙). 기존 5일치 이력은 `recommendation_engine.backfill_signal_history(drive_db)`(신설, Drive에 저장된 모든 `_recommendations_{date}.json`을 순회해 전체 재구성 — 재실행해도 안전)로 1회 백필. `app.py`는 날짜마다 따로 읽던 `get_recommendations_for_date`(v3.26)를 없애고, 리포트 히스토리 표 전체에 대해 이 누적 파일을 **단 한 번만** 읽는 `get_signal_history`로 교체 — 표를 그리는 데 필요한 Drive 읽기가 날짜 수에 비례하던 것에서 상수(1회)로 줄었다. 실제 Drive 데이터로 백필 결과(QQQ/UUP가 아직 없던 과거 날짜엔 해당 컬럼이 정상적으로 `-`)와 Streamlit AppTest 렌더링을 확인. [기능 4·5] 갱신 |
| v3.29 | 2026-07-25 | **리포트 히스토리 표를 `st.container` 카드 나열에서 `st.dataframe` 하나로, 다운로드는 별도 컴팩트 폼으로 분리.** 전체 데이터셋(날짜별 12개 티커 액션)이 이미 메모리에 다 있으니 카드를 하나씩 그리는 대신 `pandas.DataFrame` 하나로 만들어 `st.dataframe`에 넘기는 게 더 단순하고, 정렬·스크롤도 기본 지원된다. 링크 컬럼은 `st.column_config.LinkColumn(display_text=r"([^/]+)$")`로 GitHub Pages URL을 파일명만 보이는 클릭 가능한 링크로 렌더링. 액션 셀 색상은 `df.style.map(...)`(pandas Styler)로 유지(양운/음운 등에서 이미 쓰던 색상 규칙과 같은 원칙, `st.dataframe`이 Styler를 그대로 받는다). **`st.download_button`은 데이터프레임 셀에 넣을 수 있는 컬럼 타입이 아니라서** 표에서 완전히 분리 — 표 아래에 `st.form`으로 감싼 한 줄짜리 컴팩트 컨트롤(날짜 선택 `st.selectbox` + "준비" 버튼, `st.columns([3, 1])`로 나란히 배치, `label_visibility="collapsed"`로 라벨 숨겨 더 작게)을 두고, 폼 제출 후에만 실제 `st.download_button`을 표시한다 — `download_button`/`link_button`은 애초에 `st.form` 안에 넣을 수 없는 위젯이라(Streamlit 자체 제약) 폼은 "무엇을 받을지 고르는 단계"만 담당하고, 실제 다운로드 버튼은 폼 밖에서 렌더링된다. Streamlit AppTest로 데이터프레임 렌더링(14개 컬럼: 날짜+12개 티커+링크, 실제 값 확인)과 폼 제출 → 다운로드 버튼 생성까지 실제 위젯 상호작용으로 확인. [기능 4] 갱신 |
| v3.30 | 2026-07-25 | **거래량 급증 지표 설명 보완 + 지표 설명 표 컬럼 폭 조정 + 리포트 LLM 분석란 전체 폭 확장.** ① 소개 탭 지표 설명 표의 "거래량 급증" 행에 "매수/매도 판정 자체에는 관여하지 않는다(거래량이 낮아도 돌파하면 그대로 매수)"는 문장을 명시 — `get_mechanical_action`이 실제로는 `Volume_Surge`를 전혀 읽지 않고, 참고/`scan_for_signals`(미구현) 우선순위 용도로만 쓰인다는 사실이 기존 "가짜 돌파 필터"라는 표현만으로는 오해될 수 있었다. ② 같은 표를 `st.table`(컬럼 폭 제어 불가, "지표" 이름 칸이 내용 길이에 따라 과하게 넓어져 "설명" 칸이 좁아지고 줄바꿈이 잦았음)에서 직접 그린 HTML `<table>`(지표 26% : 설명 74% 고정, `word-break: keep-all`+`overflow-wrap: break-word`로 한글이 음절 중간에서 어색하게 잘리지 않도록)로 교체. ③ `report_builder.py`의 `.analysis`(개별 종목 시그널 카드의 LLM/규칙 기반 서술 텍스트) CSS에서 `max-width: 900px` 제한을 제거해 페이지 전체 폭을 쓰도록 변경 — `.overview`(교차 자산 총평)는 기존처럼 900px 제한 유지(가독성을 위해 의도적으로 남겨둔 것으로, 이번 요청은 개별 종목 분석란만 지목). Streamlit AppTest와 실제 `report_builder.build_daily_report_html` 출력으로 `.analysis`엔 `max-width`가 없고 `.overview`엔 남아있는 것을 각각 확인. [기능 2·4] 갱신 |
| v3.31 | 2026-07-25 | **일일 리포트에도 최근 30일 시그널 이력 표 추가.** v3.28에서 만든 `_signal_history.json` 누적 파일을 Streamlit 탭뿐 아니라 리포트에도 재사용 — 오늘 요약 표 바로 아래에 "최근 시그널 이력" 표(날짜 × 12개 티커, `B`/`H`/`S` 색상 표시)를 추가했다. 무제한 전체 이력이 아니라 **최근 30일(달력 기준)로만 제한**한다(`recommendation_engine._recent_signal_history`, 신설) — 리포트는 매일 새로 만들어져 git에 영구 커밋되는 "그날의 다이제스트" 성격이라, 시간이 지날수록 계속 길어지는 전체 이력을 매번 박아 넣는 건 부적절하다고 판단(전체 이력 열람은 이미 Streamlit "리포트 히스토리" 탭이 전담). `report_builder.py`는 `recommendation_engine`을 import할 수 없어(반대 방향으로 이미 import되어 있어 순환 참조가 됨) `build_daily_report_html`에 `signal_history` 인자를 새로 추가해, `paper_positions`와 동일한 방식으로 호출자(`run_asset_class_recommendations`)가 미리 로드해 전달한다. 이력이 비어 있으면(예: 신규 배포 직후) 섹션 자체를 생략한다(다른 선택적 섹션들과 동일한 원칙). 실제 Drive 데이터(2026-07-18~23, 6일치)로 표 렌더링과 빈 이력일 때 섹션 생략을 모두 확인. [기능 5·6] 갱신 |
| v3.32 | 2026-07-25 | **"리포트 히스토리" 탭에 테스트/샘플 리포트 열람용 토글 추가.** v3.27에서 테스트 발행(`config.IS_TEST_REPORT`)이 GitHub Pages에 전혀 게시되지 않도록 막고, v3.25에서 Streamlit 목록에서도 제외했더니 — 사용자가 테스트 리포트 내용을 눈으로 확인할 방법이 아예 없어지는 부작용이 생겼다(GitHub Actions 로그나 Drive를 직접 뒤지는 수밖에 없었음). `report_builder.list_test_report_dates(drive_db)`(신설, `list_report_dates`의 반대 — `_test` 접미어가 붙은 날짜만 반환)를 추가하고, 다운로드 폼 위에 "테스트/샘플 리포트도 다운로드 목록에 포함" 토글(기본 꺼짐)을 뒀다 — 켜면 다운로드 날짜 선택지에 `"{날짜} (테스트)"` 형태로 테스트 리포트들이 추가된다. 상단의 메인 표(날짜×액션×링크)에는 넣지 않았다 — 테스트 리포트는 `_signal_history.json`에도 기록되지 않고(액션 칸이 전부 `-`로만 나와 무의미) GitHub Pages URL도 없어(링크 칸이 깨짐) 표에 섞으면 오히려 헷갈리므로, 다운로드 폼에서만 접근 가능하게 했다. Streamlit AppTest로 토글 온/오프에 따른 선택지 변화와 실제 테스트 리포트 다운로드까지 실제 위젯 상호작용으로 확인. [기능 4] 갱신 |

> 과거 버전 전체 내용은 `spec_archive/investment_assistant_spec_v{N}.md` 파일로 보존합니다(v3.7부터 프로젝트 루트가 코드 파일과 뒤섞이지 않도록 `spec_archive/` 하위로 이동). 이 파일(`investment_assistant_spec.md`)은 항상 최신 버전을 담습니다.

---

## 1. 시스템 아키텍처 & 환경 (Architecture)

### 1.1 기술 스택
- **Frontend / Chat UI**: Streamlit
- **Data Source**: yfinance (Yahoo Finance API) — S&P 500 개별 종목 + 대표 자산군 ETF 프록시(아래 [기능 1] 참고)
- **Database (Virtual)**: Google Drive API + Apache Parquet (`.parquet`)
- **LLM Engine**: OpenRouter (기본 모델 `nvidia/nemotron-3-ultra-550b-a55b:free`, `OPENROUTER_MODEL_NAME` 환경변수로 교체 가능)
- **Infrastructure**: **GitHub Actions** — `collect.yml`(장 마감 후 스케줄) + `recommend.yml`(`workflow_run`으로 연쇄 실행)로 완전 자동화됨. 원래 계획은 GCP Cloud Run + Cloud Scheduler였으나 결제 계정 연결 이슈로 보류하다가, 카드 등록 없이 쓸 수 있는 GitHub Actions로 전환해 v2.8에서 실제 배포 완료 (자세한 내용은 [기능 5] 참고). Streamlit 앱은 로컬 실행 또는 별도 호스팅이며, 앱 안의 "데이터 적재" 탭 버튼은 크론과 별개로 수동 실행도 가능하게 남겨둔 것. 공개 배포처는 **GCP Compute Engine + Docker Compose**로 확정(v3.15, [GCP_Setyo_Guide.md](GCP_Setyo_Guide.md) 참고 — `docker-compose.yml`/`Dockerfile`/`.dockerignore` 포함, VM 부팅 시 시작 스크립트가 자동으로 클론·시크릿 주입·기동까지 수행), `config.STREAMLIT_ENABLE_LLM`([기능 3·4] 참고, v3.11)으로 배포 시 LLM/뉴스 API 비용을 관리한다. Cloud Run 기반의 [GCP_STREAMLIT_DEPLOY.md](GCP_STREAMLIT_DEPLOY.md)는 미검증 대안책으로 보존 중.
- **리포트 호스팅**: `recommend.yml`이 매일 생성하는 통합 일일 리포트(자세한 내용은 [기능 5] 참고)는 **GitHub Pages**(저장소 `master` 브랜치 `/docs` 폴더, `https://garam827.github.io/ai_invest_assistant/`)로 공개 발행된다 — v3.0에서 도입, 별도 계정/과금 없이 GitHub Actions와 같은 저장소 안에서 해결.
- **알림**: 텔레그램 봇으로 크론 실행 결과(매수/HOLD/매도 요약 표 + 전체 리포트 링크)를 발송 (자세한 내용은 [기능 5] 참고, 설정은 [TELEGRAM_SETUP.md](TELEGRAM_SETUP.md))

### 1.2 데이터 흐름 (Data Flow)
1. **매일 장 마감 후**: GitHub Actions `collect.yml`이 스케줄(UTC 기준 월~금 21:57 = 한국시간 화~토 06:57경, v3.10 — 아래 참고)로 가동.
2. **종목 유니버스 동기화**: 위키피디아의 현재 S&P 500 구성종목과 Drive 저장 목록을 비교(`sync_universe`). 신규 편입 종목은 5년치 백필 후 합류, 편출 종목은 데이터 보존하되 활성 목록에서 제외. 결과(활성/비활성 목록, 섹터, 종목 설명)를 `_universe.json`에 기록.
3. **yfinance API 수집**: 활성 유니버스 전 종목 + 대표 자산군 12종(`ASSET_CLASS_TICKERS` — S&P 500 1종/나스닥 1종/비트코인 1종/금 1종/미국 장·중기국채 2종/원자재 5종/달러 인덱스 1종)의 당일 OHLCV 데이터를 수집.
4. **구글 드라이브 가상 DB 적재**: OAuth 사용자 인증(캐싱된 토큰)을 통해 지정 폴더 내 `[Ticker].parquet` 파일에 당일 데이터를 Append 및 중복 제거(Upsert).
5. **`collect.yml` 성공 시 `recommend.yml`이 `workflow_run`으로 자동 연쇄 실행**. 대표 자산군 12종에 한해(S&P 500 개별 종목 제외):
   a. **신선도 게이트**: 종목별 마지막 저장일이 4일 이상 지났으면 그 종목은 건너뜀(부분 실패 대응).
   b. **시그널 엔진 작동 + 기계적 판정**: Donchian Channel/ATR을 계산해 `signal_engine.get_mechanical_action`으로 매수/HOLD/매도를 네트워크 호출 없이 결정론적으로 산출.
   c. **HOLD면 여기서 종료** (뉴스/LLM 호출 없음). **매수/매도면**: Exa API로 최근 뉴스를 수집하고 OpenRouter LLM(Nemotron)에 뉴스+시그널 상태를 근거로 서술형 설명을 요청 — 실패하면 규칙 기반 설명으로 자동 대체(판정 자체는 항상 확정됨).
   d. **통합 일일 리포트 생성 (`report_builder.py`, v3.0)**: 12종목 전체 요약 표 + LLM 교차 자산 총평(`generate_portfolio_overview`) + 종목별 캔들차트를 HTML 한 장으로 합쳐, Drive(`_report_{date}.html`)와 로컬 `docs/reports/{date}.html`에 저장. 수동으로 재실행한 샘플/테스트 발행은 `config.IS_TEST_REPORT`(v3.20, `recommend.yml`의 `workflow_dispatch` 입력 `is_test`, 기본값 `true`)가 켜져 있으면 파일명 끝에 `_test`가 붙어(`_report_{date}_test.html`/`docs/reports/{date}_test.html`) 그날의 실제 스케줄 리포트를 덮어쓰지 않는다 — 매일 자동 실행되는 `workflow_run` 경로에는 이 입력이 없어 항상 실제 파일명을 쓴다.
   e. **GitHub Pages 발행**: `recommend.yml`이 `docs/reports/{date}.html`(또는 테스트 발행이면 `{date}_test.html`)을 커밋+푸시 — 저장소에 미리 연결해 둔 GitHub Pages가 그 커밋을 자동 반영해 공개.
   f. **텔레그램 발송**: 12종목 전체를 티커/구분/액션/종가 표로 정리한 요약 텍스트(v2.9, 등폭 코드블록) + 위 리포트 URL 링크를 한 메시지로 발송(v3.0부터 종목별 차트 이미지 첨부는 하지 않음 — 차트는 링크된 리포트 안에 이미 다 있음).
6. **사용자 UI**: Streamlit 대시보드에서 대표 자산군/S&P 500 개별 종목을 언제든 대화형으로 조회·분석(크론과 별개, 텔레그램 미발송). "리포트 히스토리" 탭에서는 Drive에 저장된 과거 통합 리포트를 재계산 없이 그대로 다시 볼 수 있다.

---

## 2. 세부 기능 개발 가이드라인 (Vibe Coding용)

### [기능 1] 구글 드라이브 연동 및 데이터 수집 엔진
- **인증 방식**: OAuth 사용자 인증(데스크톱 앱 클라이언트, `client_secret.json`)으로 사용자 본인 구글 계정에 최초 1회 동의를 받고, 발급된 리프레시 토큰을 `token.json`에 캐싱하여 이후 무인 재인증. (서비스 계정 키는 조직 정책 `iam.disableServiceAccountKeyCreation`으로 발급이 차단되어 사용 불가 — 상세 배경 및 설정 절차는 [GOOGLE_DRIVE_SETUP.md](GOOGLE_DRIVE_SETUP.md) 참고)
- **알려진 함정**: OAuth 동의 화면이 "테스트" 게시 상태일 때, 로그인하려는 구글 계정이 "테스트 사용자" 목록에 등록되어 있지 않으면 `액세스 차단됨: <앱 이름>은(는) Google 인증 절차를 완료하지 않았습니다` 오류로 로그인 자체가 막힌다. OAuth 동의 화면에서 로그인에 쓸 계정을 테스트 사용자로 반드시 먼저 추가할 것.
- **초기 적재 (Initial Ingestion)**: 첫 실행 시 S&P 500 전 종목의 **최근 5년 치 일봉 데이터**를 수집하여 종목별 `[Ticker].parquet` 파일로 저장 후 드라이브 지정 폴더에 업로드.
- **일별 업데이트**: 매일 신규 데이터를 가져와 기존 파르케 파일 하단에 병합(Concat)하고, 날짜 기준 중복 데이터를 제거(`drop_duplicates`)하여 저장. 조회 시작일은 "오늘 기준 N일 전"이 아니라 **해당 종목에 저장된 마지막 날짜 기준 N일 전**으로 잡는다 — 크론이 며칠 못 돌았어도 다음 성공 실행이 그 갭을 전부 다시 채우는 자가 치유 구조.
- **종목 유니버스 동기화 (S&P 500 편입/편출 대응)**: 일별 업데이트 실행 전 `sync_universe`가 위키피디아 최신 구성종목과 Drive 저장 목록을 diff한다.
  - *신규 편입*: Drive에 파일이 없는 종목은 `run_initial_ingestion`으로 5년치를 먼저 백필한 뒤 활성 목록에 포함 (Donchian 100일/ATR 계산에 필요한 히스토리 확보 목적).
  - *편출*: 더 이상 S&P 500이 아닌 종목은 **Parquet 파일을 삭제하지 않고 보존**(과거 데이터 리서치 가치)하되, 활성 목록에서 제외하여 이후 일별 업데이트와 시그널 스캔 대상에서 빠지게 한다.
  - 결과는 Drive 폴더 내 `_universe.json`(`active_tickers`, `inactive_tickers`, `sectors`, `descriptions`, `synced_at`)에 기록되며, 이후 모든 일별 업데이트/시그널 대시보드는 `list_tickers()`가 아니라 이 `active_tickers`를 종목 유니버스의 기준으로 삼는다. `sectors`는 위키피디아 테이블의 GICS Sector 컬럼을 함께 수집한 종목→섹터 매핑으로, Streamlit 종목 차트 페이지의 섹터 필터에 쓰인다. `descriptions`는 아래 자산군 설명 항목 참고.
- **대표 자산군 ETF/현물 수집 (S&P 500 개별 종목 외 자산군 확장)**: S&P 500 지수, 나스닥 지수, 비트코인, 금, 미국 국채, 원자재는 원지수·현물을 직접 거래할 수 없거나(또는 거래량 데이터가 부실하거나) yfinance에서 안정적으로 조회하기 어려운 경우가 있어, 유동성이 높고 거래량 데이터가 확실한 **ETF(또는 BTC-USD 현물) 프록시**로 대신 추적한다.
  | 카테고리 | 자산 | 티커 |
  | --- | --- | --- |
  | 주식 | S&P 500 (미국 대형주) | `SPY` |
  | 주식 | 나스닥 100 (Nasdaq-100, 기술주 비중 높음) | `QQQ` |
  | 암호화폐 | 비트코인 | `BTC-USD` |
  | 귀금속 | 금 (Gold) | `GLD` |
  | 채권 | 미국 장기국채 (20년+) | `TLT` |
  | 채권 | 미국 중기국채 (7-10년) | `IEF` |
  | 원자재 | 원자재 종합 (Broad Commodities) | `DBC` |
  | 원자재 | 원유 (Crude Oil) | `USO` |
  | 원자재 | 천연가스 (Natural Gas) | `UNG` |
  | 원자재 | 농산물 (Agriculture) | `DBA` |
  | 원자재 | 구리 (Copper) | `CPER` |
  | 통화 | 미국 달러 인덱스 (US Dollar Index) | `UUP` |
  - `data_fetcher.ASSET_CLASS_TICKERS`에 `{티커: {"label": 표시명, "category": 카테고리, "description": 한 줄 설명}}` 구조의 고정 목록(12종 모두 큐레이션된 설명 포함)으로 관리하며, `run_asset_class_update`가 초기 백필/일별 업데이트를 S&P 500과 동일한 로직(`_update_one_ticker`)으로 처리한다.
  - S&P 500 개별 종목의 설명은 별도 큐레이션 없이, `sync_universe`가 위키피디아 테이블에서 이미 긁고 있는 **Security(회사명)** + **GICS Sub-Industry(세부업종)** 컬럼을 `"{회사명} ({세부업종})"` 형식으로 조합해 `_universe.json`의 `descriptions`에 함께 저장한다 (추가 요청 없이 기존 수집 데이터 재활용). Streamlit 종목 차트 탭과 [기능 5]의 통합 일일 리포트 요약 표 모두 이 설명을 부제/자산명으로 노출한다.
  - S&P 500 편입/편출 diff 대상(`sync_universe`)에서는 명시적으로 제외한다 — 이 자산들은 애초에 S&P 500 구성종목이 아니므로, 제외하지 않으면 매번 "편출 종목"으로 잘못 분류된다.
  - 원자재는 `DBC`(종합) 하나만으로는 개별 원자재 등락을 알 수 없어, 에너지(원유·천연가스)·농산물·산업금속(구리) 대표 종목으로 세분화했다. 구리는 경기 선행지표로도 흔히 참고된다.
- **뉴스 아카이브 (`news_fetcher.archive_news`)**: LLM 매매 추천에 사용된 뉴스 기사를 Drive에 **날짜별 JSON 파일**(`_news_{YYYY-MM-DD}.json`, `{티커: [뉴스, ...]}` 구조)로 저장한다. 기사 **링크**(없으면 제목)를 키로 중복을 검사해, 같은 날 같은 기사를 이미 저장했다면 다시 적재하지 않는다. `DriveDB.load_json`/`save_json`(범용 JSON 메타데이터 저장, `_universe.json`과 동일한 메커니즘)을 재사용해 별도 저장소 계층 추가 없이 구현.

### [기능 2] 톰 바소식 추세추종 시그널 & 포지션 사이징 엔진
`signal_engine.py`는 종목 유형을 가리지 않는 순수 계산 엔진이다 — OHLCV 데이터프레임만 주어지면 개별 S&P 500 종목이든 대표 자산군 ETF/현물(`ASSET_CLASS_TICKERS`, [기능 1] 참고)이든 동일한 규칙을 그대로 적용한다. S&P 500 지수 자체에 대한 추세추종 매매 전략도, 나아가 비트코인·채권·원자재에 대한 전략도 이런 방식(유동성 높은 프록시 + 동일 규칙)으로 세울 수 있다는 것이 v2.5~v2.6의 결론이다.

- **돈천 채널 (Donchian Channel)**: N일 기준(기본값 20일 및 100일 듀얼 셋) 최고가 및 최저가 밴드 계산.
  - *매수 시그널*: 당일 종가가 N일 최고가를 상향 돌파 시 발생.
  - *매도/청산 시그널*: 당일 종가가 최근 최고가 대비 정해진 추적 손절 라인을 하향 돌파 시 발생.
- **ATR (Average True Range)**: 14일 웰스 와일더(Wells Wilder) 이동평균 방식으로 변동성 계산.
- **볼린저 밴드 (Bollinger Bands, 참고용 보조 지표)**: 20일 SMA ± 2표준편차. 매수/매도 규칙에 직접 관여하지 않고, 차트에서 단기 과매수/과매도·변동성 스퀴즈를 참고하는 용도로만 표시.
- **일목균형표 (Ichimoku Kinko Hyo, 참고용 보조 지표 + 어드바이저리 노트, v3.16)**: 전환선(9일)/기준선(26일)/구름(선행스팬A·B, 26일 선행)/후행스팬(26일 후행)을 계산(`calculate_ichimoku`). 볼린저 밴드와 마찬가지로 매수/매도 규칙과 포지션 사이징에 직접 관여하지 않는다. 다만 `get_ichimoku_confluence(summary, action)`가 오늘 종가와 구름의 위치·방향이 기계적 판정과 일치하는지만 별도로 계산해, `recommendation_engine`의 서술 텍스트(규칙 기반 폴백 및 LLM 프롬프트 양쪽)에 참고 문장 하나로만 반영된다 — 판정 자체나 매수 수량 계산식에는 어떤 경우에도 영향을 주지 않는다.
- **매수/청산 "최초 발생일" 마킹**: 돌파/청산 조건은 추세가 지속되는 동안 여러 날 연속으로 참(True)일 수 있어, 매일 반복 표시하면 차트가 지저분해진다. `Buy_Trigger`/`Sell_Trigger` 컬럼은 그 조건이 전날 대비 새로 발생한 날에만 참이 되도록 계산하여, 차트에 "언제 진입/청산했으면 좋았을지"를 과거 데이터 위에 마커로 표시하는 데 쓰인다.
- **스톱 오더 및 포지션 사이징 공식**:
  - *진입 이후 최고가 기준 추적 손절 라인*: $\text{최고가} - (3 \times \text{ATR})$
  - *매수 가능 수량*:
    $$\text{매수 수량} = \frac{\text{사용자 설정 총 자산} \times \text{리스크 비율(기본 1%)}}{\text{손절 폭 }(3 \times \text{ATR})}$$

### [기능 3] 뉴스 스크래핑 및 '평온함(Serenity)' LLM 분석
- **뉴스 수집 (Exa API)**: 분석 대상 종목/자산의 최근 뉴스를 **Exa 검색 API**(`https://api.exa.ai/search`, `news_fetcher.fetch_ticker_news_exa`)로 수집한다. 쿼리는 기본 `"{티커} stock news"`, `category: "news"`로 뉴스 사이트에 한정, 기본 최근 7일(`config.EXA_NEWS_LOOKBACK_DAYS`)로 검색하고 `contents.summary`로 요약도 함께 받는다. 응답의 `author`가 없으면 기사 URL 도메인을 발행처로 대체한다. 인증은 `x-api-key` 헤더에 `EXA_API_KEY`. (이전에는 yfinance 내장 뉴스 피드를 썼으나 v2.7에서 Exa로 전환, v2.8에서 옛 `news_fetcher.fetch_ticker_news`/`fetch_news_for_tickers`를 코드에서 완전히 삭제했다.)
  - **자산군 프록시는 매크로 지향 쿼리로 대체 (v3.3)**: `ASSET_CLASS_TICKERS`의 각 항목에 `news_query`(영문) 필드가 있으면 `recommendation_engine`이 티커 대신 이 쿼리로 검색한다. `"GLD stock news"`처럼 ETF 티커 그대로 검색하면 "GLD 몇 % 하락"류의 단순 가격/시세 기사만 나오고 그 등락을 설명하는 매크로 뉴스는 거의 안 나오기 때문 — `"gold price outlook macro drivers analysis"`처럼 프록시가 추종하는 실물 자산 이름 + 매크로 키워드로 바꾸면 금리·중앙은행 수요·지정학 리스크 등 실제로 추세 판단에 쓸 만한 기사가 나온다(로컬 비교 테스트로 확인). S&P 500 개별 종목처럼 `news_query`가 없는 티커는 그대로 `"{티커} stock news"`를 쓴다 — 티커 자체가 회사이므로 문제가 없다.
- **LLM 프롬프트 가이드 (OpenRouter 연동)**:
  > "너는 전설적인 시스템 트레이더이자 미스터 세레니티(Mr. Serenity)로 불리는 톰 바소다. 다음 수집된 [종목 뉴스]를 분석하여 시장의 단기적인 탐욕이나 공포(노이즈)를 철저히 배제하라. 오직 이 뉴스가 장기 추세를 강화하는 팩트인지, 아니면 무시해도 되는 소음인지 평온하고 이성적인 시각으로 요약 브리핑을 작성하라."
- **인증 방식**: OpenRouter API 키(`OPENROUTER_API_KEY`)를 헤더에 담아 OpenAI 호환 `/chat/completions` 엔드포인트로 직접 호출 (별도 SDK 불필요).
- **모델**: 기본값은 무료 티어 `nvidia/nemotron-3-ultra-550b-a55b:free`. 무료 모델은 요청 빈도 제한/가용성 변동이 있을 수 있으므로, 브리핑 생성 실패 시 재시도 없이 스킵하고 다음 종목으로 진행할 것.
- **매매 추천 (`generate_recommendation`, Streamlit 차트 탭과 [기능 5]의 일일 리포트 양쪽에서 사용)**: 뉴스 노이즈 필터링 브리핑과는 별도로, 뉴스 + 현재 시그널 상태(종가, ATR, 20일/100일 돌파 여부, 트레일링 스탑, 청산 시그널, 거래량 급증 여부)를 함께 프롬프트에 담아 **매수 / HOLD / 매도** 중 하나를 결론으로 요구한다.
  - 시스템 프롬프트는 "뉴스의 감정이나 예측으로 판단하지 말고, 시그널 상태와 뉴스가 그 추세를 뒷받침하는 팩트인지만 걸러 규칙에 따라 결론 내려라"로 고정되어, 순수 뉴스 브리핑과 동일하게 노이즈를 배제하는 톤을 유지한다.
  - 응답 첫 줄은 반드시 `추천: 매수` / `추천: HOLD` / `추천: 매도` 형식으로 시작하도록 지시하고, 코드에서 정규식으로 파싱해 UI에 색상(초록/회색/빨강) 배지로 표시한다. 파싱 실패 시 안전하게 `HOLD`로 처리.
  - Streamlit에서는 종목 차트의 "조회" 버튼을 누를 때 함께 실행되며, 매 rerun마다 재호출되지 않도록 `(티커, 최신 데이터 날짜)`를 캐시 키로 하고 **TTL 24시간**으로 캐싱한다 — `st.tabs`는 보이지 않는 탭의 코드도 매 rerun마다 실행되므로, 이 캐싱이 없으면 다른 탭에서의 조작만으로도 LLM이 반복 호출된다.
  - 분석에 사용한 뉴스 기사는 카드(제목·링크, 발행처·발행시각, 요약) 형태로 표시하고(Streamlit UI, 그리고 v3.1부터는 크론이 만드는 일일 리포트의 매수/매도 시그널 카드에도 동일하게), 동시에 Drive에 날짜별로 아카이브한다 ([기능 1]의 `news_fetcher.archive_news` 참고) — 무엇을 근거로 그 추천이 나왔는지 사후에 검증 가능하도록. 크론 경로(`recommendation_engine.run_asset_class_recommendations`)에서 계산된 `text`/`news`는 `results` 딕셔너리에 담겨 그대로 `report_builder.py`로 전달되므로, 리포트를 만들기 위해 뉴스/LLM을 다시 호출하지는 않는다.
  - **뉴스 읽기 캐시 (`news_fetcher.get_cached_news`, v3.11)**: Exa를 호출하기 전에 그날치 `_news_{date}.json` 아카이브에 해당 티커가 이미 있는지 먼저 확인 — 있으면 그대로 재사용하고 없을 때만 Exa를 호출한다. 같은 종목/날짜를 여러 사용자(Streamlit 공개 배포 시)나 여러 번의 재실행이 요청해도 Exa 호출은 한 번으로 줄어든다. 크론 경로에도 동일하게 적용.
  - **LLM 선택적 비활성화 (`get_recommendation_for_ticker`의 `use_llm` 파라미터, v3.11)**: `use_llm=False`면 뉴스는 그대로 수집(위 캐시 포함)하되 OpenRouter 호출은 생략하고 `_build_rule_based_explanation`으로 대체한다 — 뉴스/LLM 호출이 실패했을 때와 같은 함수를 재사용하지만, 문구는 "LLM 분석 실패"가 아니라 "LLM 분석 비활성화"로 구분해 보여준다(의도적 비활성화와 장애를 혼동하지 않도록). Streamlit 공개 배포([기능 4] 참고)가 이 파라미터의 주 사용처.

### [기능 4] Streamlit 웹 대시보드 및 챗봇 인터페이스
단일 페이지(`app.py`)에 `st.tabs`로 화면을 구분한다 (사이드바 멀티페이지 `pages/` 구조는 v2.5에서 제거 — 사용자가 명시적으로 페이지 대신 탭을 요청). 페이지 최상단의 앱 전체 제목(`st.title`)은 v2.7에서 삭제했다 — "소개" 탭 안의 헤더로만 남아있다.

**탭 1: 소개 (구현 완료)** — 분석 기능 없이 설명만 제공. 실제 지표 분석은 탭 2부터 시작한다.
- **이 프로그램에 대하여** (프로그램 목표 + 톰 바소 투자 철학 설명, expander로 항상 펼쳐진 상태)
- **사용된 지표 설명** ([기능 2]의 지표들 — 개별 차트마다 반복 표시하지 않고 이 탭에서만 한 번 제공). 컬럼 폭을 직접 제어할 수 없는 `st.table` 대신, 지표:설명 26%:74% 고정 비율 + 한글 어색한 줄바꿈 방지(`word-break: keep-all`)를 적용한 커스텀 HTML `<table>`을 `st.markdown(unsafe_allow_html=True)`으로 직접 그린다(v3.30).

**탭 2: 대표 자산군 분석 (구현 완료)** — S&P 500·나스닥 자체(및 비트코인/금/미국채/원자재/달러 인덱스)에 대한 추세추종 전략. [기능 1]의 `ASSET_CLASS_TICKERS`를 카테고리(주식/암호화폐/귀금속/채권/원자재/통화) → 세부 종목 2단계로 선택(탭 3의 섹터→종목 패턴과 동일). 카테고리 변경은 즉시 반영되고, 세부 종목·기간 선택 후 "조회"를 눌러야 차트가 렌더링된다.

**탭 3: 종목 차트 (S&P 500) (구현 완료)** — 개별 종목 상세 뷰. 섹터(GICS Sector) → 종목 → 표시 기간(기본 6개월) 순으로 선택. 섹터 변경은 즉시 종목 목록에 반영되지만, 실제 조회(Drive 로드 + 지표 계산 + 차트 렌더링)는 폼으로 감싸 "조회" 버튼을 눌러야만 실행됨(불필요한 재계산 방지). 지표 설명은 탭 1에만 있고 이 탭에서는 반복하지 않는다.

탭 2·탭 3은 동일한 차트 렌더러(`render_ticker_chart`)를 공유한다:
- 캔들차트(plotly) 위에 볼린저 밴드(음영, v3.19에서 더 연하게 조정), Donchian 20일/100일 밴드, 트레일링 스탑, 일목균형표 구름(선행스팬A·B)을 라인/음영으로 오버레이. 거래량(급증일 강조)과 ATR은 스케일이 달라 별도 서브플롯으로 분리, 캔들:거래량:ATR 높이 비율은 **4:1:1**(v3.12에서 2:1:1로 바꿨다가 v3.13에서 3:1:1로 원복, v3.17에서 4:1:1로 재조정). 범례는 `BB`/`DC20`처럼 축약된 이름을 쓴다(v3.12) — 전체 뜻 설명은 이 탭의 지표 설명 표(위 참고)에 이미 있으므로 반복하지 않는다. 범례는 차트 우측 세로 고정폭이 아니라 **하단 가로 배치**이며(v3.18 — 모바일 등 좁은 화면에서 세로 범례가 차트 폭을 잠식하는 문제 해소), 가격/거래량/ATR은 자기 (서브)플롯으로 이미 뜻이 명확해 범례에서 제외하고, BB/DC20/DC100은 상단·하단 두 트레이스를 하나의 범례 항목으로 묶어(`legendgroup`, 클릭 시 그룹 전체 토글) 표시한다 — 결과적으로 범례 항목은 BB·DC20·DC100·손절선·양운·음운·매수·매도 8개(v3.19에서 전환선/기준선/후행스팬 라인을 차트에서 제거하면서 11개→8개로 더 줄었다). 일목균형표는 구름(선행스팬A·B)만 표시하며(v3.19 — 전환선/기준선/후행스팬은 `signal_engine`에서 여전히 계산되지만 차트에는 그리지 않는다), 마지막 캔들 이후 26영업일 미래까지 뻗어나가는 전통적인 방식은 유지(v3.16) — `chart_builder`가 표시 구간 마지막 26개 원본(비변위) 값을 향후 26영업일에 이어붙여 트레이스를 구성, 거래량/ATR 서브플롯은 실제 데이터가 끝나는 지점에서 그대로 멈춘다(정상 동작). 구름은 선행스팬A≥B인 구간(양운, 상승 추세)은 연한 붉은색, 그 반대(음운)는 연한 파란색으로 구간별로 나눠 채색한다(v3.19) — Plotly 트레이스 하나로는 조건부 채움색을 줄 수 없어 부호가 바뀌는 지점마다 별도 트레이스 쌍으로 나눠 그린다.
- **과거 매수/청산 시그널 마킹**: `Buy_Trigger`/`Sell_Trigger`가 참인 날에 초록 세모(▲)/빨간 세모(▼) 마커를 표시해, 과거 데이터 위에서 톰 바소 규칙대로라면 언제 매수·청산했을지 시각적으로 보여줌.
- 상단에 종목명(또는 자산군명)·부제(섹터 또는 자산군 설명)·현재가(전일 대비 등락 색상 표시)·ATR·트레일링 스탑·오늘 시그널 요약.
- **LLM 매매 추천**: "조회" 버튼을 누르면 차트 렌더링과 함께 `openrouter_briefing.generate_recommendation`이 그 종목의 당일 뉴스 + 현재 시그널 상태를 근거로 매수/HOLD/매도를 추천하고, 색상 배지(초록/회색/빨강)와 근거 텍스트로 표시. `(티커, 최신 날짜, use_llm)` 기준, TTL 24시간으로 캐싱되어 동일 데이터에 대해 반복 호출되지 않음.
  - **공개 배포 모드 (`config.STREAMLIT_ENABLE_LLM`, v3.11)**: 이 값이 `false`면(로컬 개발 기본값은 `true`라 영향 없음) "조회"를 눌러도 이 섹션이 자동으로 뜨지 않고, "뉴스/분석 불러오기" 버튼을 별도로 눌러야만 실행된다 — Exa/OpenRouter API 비용을 관리하기 위한 게이트. 게이팅 상태는 티커별 `st.session_state` 키로 관리해 다른 종목을 조회하면 자동으로 다시 잠긴다. 이 모드에서는 뉴스는 수집하되 LLM 서술 분석은 생략되고 규칙 기반 설명으로 대체된다([기능 3]의 `use_llm` 파라미터 참고).
- **분석에 사용된 뉴스 카드**: 추천 아래에 근거로 쓰인 뉴스 기사를 카드(제목·링크, 발행처·발행시각, 요약)로 나열. 같은 뉴스는 Drive에도 날짜별로 아카이브됨([기능 1]/[기능 3] 참고).
- HTS(홈트레이딩시스템) 스타일: 차트 테두리, 우측 세로 범례, 마우스 오버 시 크로스헤어형 통합 툴팁(`hovermode="x unified"`), 서브플롯 간 여백 확대.

**탭 4: 데이터 적재 (구현 완료, 원래 메인 페이지 위치에서 이동)** — 버튼 하나(`전체 데이터 적재`)로 `run_full_collection`(S&P 500 유니버스 동기화 + 전 종목 갱신 + 자산군 ETF 갱신)을 실행. v2.8부터는 GitHub Actions `collect.yml`이 매일 자동으로 동일 로직을 실행하므로, 이 버튼은 크론과 별개로 원할 때 즉시 갱신하고 싶을 때 쓰는 수동 실행 경로다. 실행 중 로그를 화면에 실시간 스트리밍하고, 완료 시 활성/신규편입/편출/자산군 종목 수를 요약 표시. 완료 후 종목 목록·시세 캐시를 즉시 비워, 같은 세션에서 바로 다른 탭으로 이동해도 새 데이터가 반영됨.

**탭 5: 리포트 히스토리 (구현 완료, v3.0 신설, v3.24~v3.29에서 여러 차례 재구성)** — 크론이 매일 Drive에 저장하는 통합 일일 리포트(`_report_{date}.html`, [기능 5] 참고) 이력을 하나의 `st.dataframe`으로 보여준다(v3.29 — v3.26의 `st.container` 카드 나열에서 교체, 전체 데이터셋이 이미 메모리에 있어 표 위젯 하나가 더 단순하고 정렬·스크롤도 기본 지원). 컬럼은 날짜 + `ASSET_CLASS_TICKERS` 12종 각각의 그날 액션(매수=`B`/HOLD=`H`/매도=`S`, 데이터 없으면 `-`, `df.style.map`로 색상) + 링크(`st.column_config.LinkColumn`, GitHub Pages URL을 파일명만 보이는 클릭 가능한 링크로 렌더링). 액션 컬럼은 날짜마다 무거운 `_recommendations_{date}.json`을 따로 읽는 대신, 표 전체에 대해 경량 누적 파일 `_signal_history.json`을 단 한 번만 읽는 `get_signal_history`로 채운다(v3.28, [기능 5] 참고). 리포트 본문 자체는 GitHub Pages 링크로 새 탭에서 열어 전체 폭·단일 스크롤로 원래 설계대로 보이게 한다(v3.24 — v3.0~v3.23까지 쓰던 `st.iframe` 임베드는 좁은 Streamlit 컨테이너 폭에 다시 눌리고 이중 스크롤이 생겼었음). **다운로드**는 `st.download_button`이 데이터프레임 컬럼 타입으로 지원되지 않아 표에서 분리, 표 아래 `st.form`으로 감싼 한 줄짜리 컴팩트 컨트롤(날짜 `st.selectbox` + "준비" `st.form_submit_button`, `st.columns([3, 1])`, 라벨 숨김)로 날짜를 고르면 폼 밖에서 실제 다운로드 버튼이 나타난다(v3.29 — `download_button`/`link_button`은 Streamlit 제약상 `st.form` 안에 넣을 수 없음). 폼 위에는 "테스트/샘플 리포트도 다운로드 목록에 포함" `st.toggle`(기본 꺼짐, v3.32)이 있어 — 켜면 `report_builder.list_test_report_dates`로 `_test` 리포트도 `"{날짜} (테스트)"` 라벨로 다운로드 선택지에 추가된다(메인 표에는 안 섞임 — 액션 이력·GitHub Pages 링크 둘 다 테스트 리포트엔 없어서). v3.27로 테스트 발행이 GitHub Pages/Streamlit 목록 어디에도 안 나오게 된 이후, 사용자가 테스트 리포트 내용을 직접 확인할 유일한 경로가 이 토글이다. 날짜 목록(`report_builder.list_report_dates`)은 수동/샘플 테스트 발행(`_test` 접미어)을 자동으로 제외한다(v3.25, v3.27에서 근본 원인까지 수정). 날짜 목록은 `st.cache_data(ttl=3600)`, 신호 이력·리포트 본문은 각각 `st.cache_data(ttl=3600/86400)`로 캐싱(과거 데이터는 다시 바뀌지 않으므로 긴 TTL).

**탭 6: 모의 투자 (구현 완료, v3.21)** — 사용자가 직접 기록하는 가상의 매수 포지션과 그 손익을 추적한다. 자세한 내용은 [기능 6] 참고.

**미구현**:
- **오늘의 시그널 전체 스캔 테이블**: `signal_engine.scan_for_signals`로 전 종목을 한 번에 스캔해 돌파+거래량 급증 종목을 우선순위로 보여주는 테이블 뷰.
- **평온한 어시스턴트 (Chat)**: 사용자가 "오늘 AAPL의 추적 손절가는 얼마야?", "오늘 포지션 사이징 리스크를 2%로 올리면 어떻게 해야 해?"라고 질문하면 가상 DB의 파르케 데이터를 읽어 계산 후 답변하는 LLM 에이전트 창.

### [기능 5] 자동화 인프라 (GitHub Actions) & 텔레그램 알림
원래 계획이던 GCP Cloud Run + Cloud Scheduler는 결제 계정 연결 이슈로 보류되었고, 카드 등록 없이 무료로 쓸 수 있는 **GitHub Actions**로 v2.8에서 실제 무인 자동화를 완성했다. 대표 자산군 12종(`ASSET_CLASS_TICKERS`)에 한해 매일 자동으로 매수/HOLD/매도 판정과 텔레그램 알림까지 수행하며, S&P 500 개별 종목 503개는 데이터 수집만 자동화하고 추천/알림은 Streamlit에서 사용자가 직접 조회한다 (LLM/뉴스 API 사용량을 대표 자산군으로 한정해 비용·rate limit을 관리).

- **워크플로 2개, `workflow_run`으로 연쇄**:
  - `.github/workflows/collect.yml`: `cron: "57 21 * * 1-5"`(UTC 월~금 21:57) + `workflow_dispatch`(수동 실행)로 가동. `python data_fetcher.py update` 한 줄로 `sync_universe`+전 종목 갱신+자산군 ETF 갱신을 모두 수행. **요일 마스크는 UTC 기준**이라 한국시간(UTC+9)으로 환산하면 자정을 넘어가 하루씩 밀려 한국시간으로는 **화~토 06:57경**에 도착한다(v3.7에서 "월요일 아침 알림이 없다"는 이유로 UTC 일~목으로 바꿔봤으나, 재검토 결과 한국시간 월요일 아침 시점엔 어차피 새 미국 거래일 데이터가 아직 없어(주말 지난 금요일 종가 그대로) 알림을 보내봤자 중복이라는 결론으로 v3.8에서 UTC 월~금으로 원복 — 토요일 알림(금요일 종가)은 그 주의 마지막 거래일 데이터를 확인하는 의미가 있어 유지).
  - **시각을 06:57 KST 근처로 앞당김 + 정각/반각 회피 (v3.9 → v3.10)**: 기존 22:30 UTC(한국시간 07:30경)에서 21:37 UTC(06:37경)로 한 차례 앞당겼다가, 겨울철 버퍼를 조금 더 확보하고 싶어 21:57 UTC(06:57경)로 최종 조정했다(사용자가 직접 `:57`로 수정). 다만 이 때문에 EST(미국 겨울시간, 11~3월) 구간에는 장마감(16:00 ET) 후 버퍼가 **약 57분**으로 줄어든다(기존 22:30 UTC는 EST 기준 1.5시간 버퍼를 일부러 확보한 설계였음) — 그 시점에 yfinance 데이터가 아직 안 올라와 있으면 그날치만 못 가져오고 다음 실행에서 자가 치유되는 정도라 감수 가능한 트레이드오프로 판단. 분(分)도 `:57`처럼 정각/반각이 아닌 값으로 의도적으로 어긋나게 잡았다 — GitHub Actions가 `:00`/`:15`/`:30`/`:45` 같은 인기 있는 시각에 스케줄 실행을 더 많이 큐잉해 지연이 잦다는 게 알려져 있기 때문(완전히 피할 수는 없고 확률만 줄이는 수준).
  - **GitHub Actions 스케줄은 "best-effort"라 지연될 수 있음**: 실제로 22:30 UTC 목표가 23:31 UTC(약 61분 지연)에 실행된 사례를 확인했다(`gh run list`로 검증) — 정각/반각처럼 사용량이 몰리는 시각일수록 지연 가능성이 커진다. 이는 크론 표현식의 문제가 아니라 GitHub 플랫폼 자체의 알려진 특성이며, 코드로 고칠 수 있는 부분이 아니다.
  - `.github/workflows/recommend.yml`: `collect.yml`이 **성공적으로 완료됐을 때만**(`workflow_run` + `if: github.event.workflow_run.conclusion == 'success'`) 자동 연쇄 실행되거나, `workflow_dispatch`로 수동 실행 가능. `python recommendation_engine.py`로 대표 자산군 12종의 추천을 생성하고 텔레그램으로 발송.
  - **수동 테스트용 LLM/뉴스 스킵 토글 (v3.5)**: `workflow_dispatch`에 `skip_llm_and_news` 입력(boolean, 기본값 `true`)을 추가 — 켜져 있으면 `SKIP_LLM_AND_NEWS` 환경변수를 통해 `config.SKIP_LLM_AND_NEWS`가 `true`가 되고, `recommendation_engine.get_recommendation_for_ticker`와 `report_builder`의 LLM 총평 생성이 Exa/OpenRouter 호출을 건너뛰어 곧바로 규칙 기반 설명으로 대체한다. UI/파이프라인/GitHub Pages 배포만 확인하려고 반복 실행하는 수동 테스트에서 매번 실제 뉴스/LLM API를 태우지 않기 위함. `workflow_run`으로 연쇄되는 실제 스케줄 실행에는 `github.event.inputs`가 존재하지 않아 이 값이 항상 빈 문자열(=`false`)로 남으므로, 실 서비스 경로에는 영향이 없다.
  - 두 워크플로 공통으로 GitHub 저장소 Secrets 6개(`GOOGLE_OAUTH_CLIENT_SECRET_JSON`, `GOOGLE_OAUTH_TOKEN_JSON`, `DRIVE_FOLDER_ID`, `OPENROUTER_API_KEY`, `OPENROUTER_MODEL_NAME`, `EXA_API_KEY`)를 환경변수로 주입하며, `recommend.yml`은 텔레그램 발송을 위해 `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` 2개를 추가로 주입해 총 8개를 쓴다(`collect.yml`은 6개만).
- **데이터 신선도 게이트 (`recommendation_engine._is_data_fresh`)**: `collect.yml`이 전체적으로는 성공(exit 0)해도, `data_fetcher`의 종목별 `try/except`로 인해 일부 종목만 조용히 실패했을 수 있다. 종목별 마지막 저장일이 오늘 기준 `config.DATA_FRESHNESS_MAX_AGE_DAYS`(기본 4일)를 넘으면 그 종목은 추천 생성을 건너뛰어, 오래된 데이터로 잘못된 매수/매도 판정을 내리는 것을 방지한다.
- **매수/HOLD/매도 판정의 기계화 (`signal_engine.get_mechanical_action`)**: 판정은 항상 Donchian 돌파/트레일링 스탑 이탈 여부만으로 네트워크 호출 없이 결정론적으로 나온다 — LLM이나 뉴스 API 상태와 무관하게 항상 확정된다.
  - **HOLD인 날은 뉴스 수집·LLM 호출을 아예 생략**한다 (하루 대부분은 HOLD이므로 API 사용량이 크게 준다).
  - **매수/매도인 날**: Exa로 뉴스를 수집하고 OpenRouter LLM에 뉴스+시그널 상태로 서술형 설명을 요청한다. LLM이 반환한 액션이 기계적 판정과 다르면 로그만 남기고 **기계적 판정을 그대로 사용**한다(액션은 LLM이 바꿀 수 없음).
  - **LLM/뉴스 호출이 실패해도** (실제 OpenRouter 무료 모델 429 상황으로 검증됨) 판정 자체는 절대 유실되지 않는다 — `_build_rule_based_explanation`이 "어떤 숫자 규칙이 발동했는지"를 명시한 결정론적 설명 텍스트로 자동 대체한다.
  - 그날의 전체 판정 결과는 Drive에 `_recommendations_{YYYY-MM-DD}.json`으로도 저장된다(`run_asset_class_recommendations`) — 텔레그램 발송이 실패하더라도 판정 이력 자체는 남는다. **신호 이력 누적 (`_signal_history.json`, v3.28)**: 실제(비-테스트) 발행마다 `{날짜: {티커: 액션}}` 형태로 그날의 액션만 추가로 기록하는 경량 파일 — Streamlit "리포트 히스토리" 표가 날짜마다 무거운 `_recommendations_{date}.json`을 따로 읽지 않고 이 파일 하나만 읽도록 하기 위한 용도. `config.IS_TEST_REPORT`일 때는 갱신을 건너뛴다. `recommendation_engine.backfill_signal_history(drive_db)`로 기존 `_recommendations_*.json` 전체에서 언제든 재구성 가능(재실행해도 안전, 매번 전체 재구성). **일일 리포트에도 재사용 (v3.31)**: `_recent_signal_history(history, days=30)`로 최근 30일치만 잘라 `report_builder.build_daily_report_html`의 `signal_history` 인자로 전달 — 리포트 요약 표 바로 아래에 같은 데이터로 "최근 시그널 이력" 표를 추가한다. 전체 이력이 아니라 30일로 제한하는 이유는 리포트가 매일 새로 만들어져 git에 영구 커밋되는 "그날의 다이제스트"이기 때문 — 전체 이력 열람은 Streamlit 탭이 전담하고, 리포트에는 최근 맥락만 곁들인다.
- **통합 일일 리포트 (`report_builder.py`, v3.0 신설)**: 그날의 12종목 판정 결과 하나로 HTML 리포트 한 장을 만든다(`build_daily_report_html`).
  - **LLM 교차 자산 총평 (`openrouter_briefing.generate_portfolio_overview`)**: 개별 종목 판정과는 별개로, 12종목의 액션/종가를 한 번에 프롬프트에 담아 "여러 자산군에 걸쳐 동시에 나타나는 추세"만 감정 없이 한 문단으로 종합 해설하도록 요청한다. 시스템 프롬프트에 "판정을 절대 바꾸거나 재해석하지 않는다"를 명시해, 이 총평이 개별 종목의 매수/HOLD/매도 판정에 영향을 주지 않도록 못 박는다(기계적 판정과 완전히 분리된 해설 전용 — [기능 3]의 `generate_recommendation`과 동일한 원칙). LLM 호출이 실패해도 총평 없이 표+차트만으로 리포트 생성 자체는 계속 진행된다.
  - **요약 표**: 티커/자산/액션/종가 HTML 표. 매수/매도 종목의 티커 셀은 아래 시그널 카드로 점프하는 앵커 링크(`#ticker-{티커}`)가 된다. **카테고리별 그룹화 (v3.5)**: `results`가 `ASSET_CLASS_TICKERS` 정의 순서를 그대로 따라 같은 카테고리 종목이 이미 연속으로 붙어 있으므로, 카테고리가 바뀌는 지점마다 `colspan` 그룹 헤더 행("주식"/"암호화폐"/"귀금속"/"채권"/"원자재")을 끼워 넣어 표 맨 위부터 자산군별로 한눈에 구분되게 했다 — 별도 정렬 로직 없이 구현 가능했고, 반복되던 "구분" 컬럼은 그룹 헤더로 대체되어 표에서 제거했다.
  - **최근 시그널 이력 표 (v3.31)**: 요약 표 바로 아래에 최근 30일(달력 기준)의 날짜×12개 티커 액션 표(`B`/`H`/`S`, 색상 표시) — `_signal_history.json`([기능 5] "신호 이력 누적" 참고)을 그대로 재사용. 이력이 없으면(신규 배포 직후 등) 섹션 자체를 생략한다.
  - **매수/매도 시그널 카드 (v3.1)**: 매수/매도 종목마다 `<section class="signal-card">` 하나씩 — 액션 배지(색상은 요약 표와 동일), `get_recommendation_for_ticker`가 이미 계산해 둔 LLM 서술 분석(또는 규칙 기반 폴백 텍스트, [기능 3] 참고)을 그대로 표시, 그 아래 참고 뉴스 카드(제목·링크·발행처·요약, `results[ticker]['news']`를 재사용 — 뉴스를 다시 수집하지 않음). 차트는 `<details><summary>📈 차트 보기</summary>...</details>` 접이식 위젯 안에 넣어 기본은 접힌 상태로 시작 — 분석/뉴스 텍스트가 먼저 보이고 차트는 필요할 때만 펼치게 해 페이지 첫인상을 가볍게 유지한다.
  - **HOLD 종목**: 뉴스/LLM 분석이 없으므로(HOLD인 날은 [기능 5]의 신선도 게이트 이후 단계에서 애초에 뉴스/LLM 호출 자체를 생략) 개별 카드 대신, 페이지 맨 아래 하나의 접힌 `<details>` 블록("HOLD 종목 차트 보기 (N개, 참고용)")에 차트만 모아 둔다. 시그널 카드에 이미 차트가 있는 종목은 이 블록에서 제외해 같은 차트를 두 번 그리지 않는다.
  - 모든 차트는 `chart_builder.build_ticker_chart_figure` → Plotly `fig.to_html(include_plotlyjs=...)`로 임베드한다(리포트 전체에서 첫 차트만 Plotly JS를 CDN에서 로드, 나머지는 재사용). 브라우저가 클라이언트 사이드에서 렌더링하므로 서버 쪽 이미지 렌더링(kaleido 등)이 전혀 필요 없다 — 아래 참고. 각 차트는 `.chart-frame`(테두리+패딩)으로 감싸 시각적으로 구분하고(v3.12), 페이지 우상단에는 차트 범례 약어(`BB`/`DC20`/`DC100`/`손절선`)를 풀어 설명하는 작은 안내 문구를 3줄로 나눠 우측 정렬로 둔다(`.chart-legend-key`, v3.12 도입 → v3.13에서 한 줄 나열 대신 줄바꿈).
  - **XSS/마크업 깨짐 방지**: LLM 분석 텍스트와 Exa 기사 제목/요약처럼 리포트가 직접 생성하지 않은 외부 텍스트는 모두 `html.escape()`로 이스케이프한 뒤 삽입한다 — LLM 응답에 `<`/`>` 등이 섞여도 리포트 마크업이 깨지지 않도록.
  - **데스크톱 여백 (v3.3, v3.4에서 추가 조정, v3.30에서 `.analysis`만 재조정)**: 처음엔 컨테이너 `max-width`를 1000px → 1400px로 넓혔으나 큰 모니터에서는 여전히 여백이 많다는 피드백을 받아, v3.4에서 `max-width` 제한 자체를 없애고 `width: 97%`로 화면 폭에 맞춰 늘어나도록 재조정했다. 표·차트·뉴스 카드는 이 넓은 폭을 그대로 쓰고, 교차 자산 총평(`.overview`)은 줄 길이가 너무 길어지면 읽기 불편해지므로 `max-width: 900px`로 계속 제한한다. 개별 종목 LLM/규칙 기반 분석 문단(`.analysis`)은 v3.4~v3.29까지 `.overview`와 같은 900px 제한을 공유했으나, 상대적으로 좁아 보인다는 피드백으로 v3.30에서 이 제한만 없애고 페이지 전체 폭을 쓰도록 바꿨다 — "표·차트는 화면 폭 전부, 긴 글줄은 읽기 편한 폭"이라는 기존 원칙에서, 종목별 분석은 표·차트 쪽 취급으로 넘어간 셈이다.
  - **뉴스 카드 레이아웃 (v3.3 → v3.5)**: v3.3에서는 반응형 그리드(`repeat(auto-fit, minmax(320px, 1fr))`)로 여러 열 배치를 시도했으나, 카드 하나하나가 너무 좁아 보인다는 피드백으로 v3.5에서 한 열(세로 나열)로 되돌리고 넓어진 컨테이너 폭을 가로로 길게 쓰도록 변경했다(`display: flex; flex-direction: column` — `flex-wrap`을 주지 않아 구조적으로 한 줄에 카드 하나만 나올 수밖에 없고, 각 카드에도 `width: 100%`를 명시해 flexbox 기본 스트레치 동작에 의존하지 않도록 확실히 했다). 카드 폰트도 `0.88rem`(메타 정보는 `0.8rem`)로 살짝 줄여 정보 밀도를 높였다.
  - **저장 위치 2곳**: (a) Drive에 `_report_{date}.html`로 저장 — Streamlit "리포트 히스토리" 탭이 재계산 없이 그대로 다시 보여줌. (b) 로컬 `docs/reports/{date}.html`로도 저장 — `recommend.yml`이 이 파일을 커밋+푸시해 GitHub Pages가 공개 서빙(`https://garam827.github.io/ai_invest_assistant/reports/{date}.html`). 결과가 비어 있으면(전 종목 신선도 게이트 탈락 등) 리포트 생성 자체를 건너뛰고, `docs/reports` 디렉터리가 없으면 `recommend.yml`의 발행 스텝도 조용히 스킵한다. **(b)는 실제 발행(`date`가 `_test`로 끝나지 않을 때)에만 해당** — `config.IS_TEST_REPORT`가 켜진 테스트 발행은 (a) Drive에만 저장되고 (b) 로컬 파일 쓰기 자체를 건너뛴다(v3.27, `report_builder.save_report`) — 그래야 테스트 발행이 git 커밋이나 GitHub Pages에 전혀 닿지 않는다. `report_url`(텔레그램 링크용)도 `IS_TEST_REPORT`일 때는 아예 만들지 않는다(v3.27) — 게시되지 않을 URL을 안내하지 않기 위함.
  - **리포트 생성 실패는 크론 전체를 막지 않음**: LLM/Drive/Git 어느 단계에서 실패하든 예외를 잡아 로그만 남기고, 이미 계산된 매수/HOLD/매도 판정과 텔레그램 발송은 그대로 진행된다(`_recommendations_{date}.json` 저장 실패 시의 폴백과 동일한 원칙).
- **kaleido/헤드리스 렌더링 전면 제거 (v3.0)**: v2.8~v2.9에서는 텔레그램에 종목별 차트를 PNG로 첨부하기 위해 kaleido(헤드리스 Chromium)를 썼고, 이 때문에 Ubuntu 러너에 CJK 폰트를 설치해야 하는 문제가 있었다. v3.0부터는 차트가 모두 브라우저에서 JS로 렌더링되는 리포트 페이지에 임베드되므로 서버 쪽 이미지 렌더링 자체가 사라졌다 — `recommend.yml`의 `fonts-nanum` 설치 스텝과 `requirements.txt`의 `kaleido` 의존성을 삭제했다. `chart_builder.FONT_FAMILY`는 여전히 유지(브라우저 폰트 힌트 목적으로는 여전히 유용하지만, 더 이상 "설치가 필수인 CI 환경 이슈"는 아니다).
- **텔레그램 알림 (`telegram_notifier.py`, 설정은 [TELEGRAM_SETUP.md](TELEGRAM_SETUP.md))**: `recommendation_engine.run_asset_class_recommendations`가 크론 경로에서만 호출하며, Streamlit의 개별 조회(`get_recommendation_for_ticker`)에서는 절대 호출하지 않는다(그렇지 않으면 사용자가 차트를 조회할 때마다 알림이 스팸처럼 발송됨). v3.0부터 메시지 1건만 보낸다: 12종목 전체를 **표 형태**로 정리한 요약(Telegram Bot API는 어떤 parse_mode에서도 GFM/HTML 테이블을 렌더링하지 않으므로, 등폭 서체 코드블록 안에 티커/구분/액션/종가 4열을 정렬 — `unicodedata.east_asian_width`로 한글·영문 폭 차이 보정, 이모지는 표 밖 범례 한 줄로만 사용, v2.9) 끝에 위 통합 리포트의 GitHub Pages URL 링크를 덧붙인다(`format_summary(results, report_url=...)`). 종목별 차트 이미지를 개별 첨부하던 v2.8~v2.9 방식은 폐지 — 차트는 이제 링크된 리포트 안에 전부 있으므로, 하루 메시지 수가 최대 6건(요약 1 + 차트 최대 5)에서 1건으로 줄었다.
- **종목 설명 노출**: [기능 1]에서 마련한 `ASSET_CLASS_TICKERS`의 큐레이션 설명과 `_universe.json`의 위키피디아 기반 설명이 리포트 요약 표와 Streamlit 양쪽 차트 탭에 노출된다(텔레그램 요약 표는 지면상 전체 설명 대신 짧은 카테고리만 표시, v2.9).

### [기능 6] 모의 투자 (Paper Trading, v3.21)
사용자가 "특정일에 특정 종목을 종가에 매수했다"는 가상의 포지션을 기록해 두면, 이미 매일 수집되는 시세 데이터만으로 그 포지션의 손익을 자동 추적한다. 원래 별도 설계 문서(`paper_trading_spec.md`)로 미리 확정해 둔 스펙을 그대로 구현했다 — 실제 구현 시점에 이 문서로 통합하고, 원본은 `spec_archive/paper_trading_spec_v1.md`로 보존.

- **핵심 설계 결정 — 인터랙션은 Streamlit UI에서만**: 포지션 추가/청산 같은 사람의 입력이 필요한 동작은 전부 Streamlit "모의 투자" 탭에서만 처리한다. `collect.yml`/`recommend.yml` 크론은 완전 무인·읽기 전용으로 설계돼 있어(사람이 입력할 경로가 없음), 이 기능도 크론 쪽에서는 **절대 쓰기(open/close)를 하지 않고 읽기만** 한다 — 텔레그램 봇으로 명령을 받는 방식은 봇 API의 상시 리스닝 서버(webhook/polling)를 새로 구축해야 하는 훨씬 큰 작업이라 채택하지 않았다.
- **데이터 모델 (`paper_trading.py`, Drive `_paper_trades.json`)**: `{"positions": [{"id", "ticker", "quantity", "entry_date", "entry_price", "status": "open"|"closed", "exit_date", "exit_price", "note", "created_at"}, ...]}`. `id`는 `uuid.uuid4().hex[:8]`(같은 종목을 여러 번 매수/매도할 수 있어 티커만으로는 포지션을 구분할 수 없음). 부분 청산(scale-out)은 v1 스코프에서 지원하지 않음 — 포지션은 통째로 열고 통째로 닫는다.
- **가격 자동 조회**: `entry_price`/`exit_price`를 지정하지 않으면 각각 매수일/청산일(청산은 기본 오늘)의 실제 종가를 `drive_db.load_ticker`에서 자동 조회해 채운다 — 해당 날짜에 거래일이 없으면(주말/휴일을 고른 경우) 그 이전 가장 최근 거래일 종가로 대체한다. 사용자가 다른 체결가를 가정하고 싶으면 Streamlit 폼에서 직접 수정 가능.
- **손익 계산 (`compute_position_returns`)**: 보유 중(open) 포지션은 최신 종가 대비 `unrealized_pnl`(금액)/`unrealized_pnl_pct`(%), 청산된(closed) 포지션은 청산가 기준 `realized_pnl`/`realized_pnl_pct`를 계산한다. 종목 시세를 못 찾으면 `current_price`를 `None`으로 두고 예외를 던지지 않는다 — 크론 전체를 막지 않는 기존 원칙과 동일. **새로운 외부 API 호출이 전혀 없다** — yfinance/Exa/OpenRouter 어느 것도 이 기능 때문에 추가로 호출되지 않고, 이미 Drive에 있는 시세만 재사용한다.
- **대상 티커**: `data_fetcher.ASSET_CLASS_TICKERS`뿐 아니라 S&P 500 개별 종목도 포함 — 둘 다 `drive_db.load_ticker`로 조회 가능하므로 제한할 이유가 없다.
- **Streamlit UI (탭 6 "모의 투자")**: 종목 유형(대표 자산군/S&P 500 개별종목)을 라디오로 고른 뒤, 각각 탭 2(카테고리→종목)·탭 3(섹터→종목)과 동일한 2단계 선택 패턴을 그대로 재사용 — 새로운 검색/드롭다운 위젯을 따로 만들지 않았다. 매수일 선택 시 그 날짜의 제안 체결가를 미리 보여주고, 폼 제출 시 `paper_trading.open_position` 호출. 보유 포지션은 카드형(`st.container(border=True)`)으로 나열하며 손익을 초록/빨강(`:green[...]`/`:red[...]`)으로 표시하고, 카드마다 청산 폼(청산일/청산가 기본값 자동 채움)이 붙는다. 청산 내역은 접힌 `st.expander`에 표로만(조작 불가) 표시. 포지션 목록은 `st.cache_data(ttl=300)`로 캐싱하며, 열기/닫기 직후 `.clear()` + `st.rerun()`으로 즉시 반영("데이터 적재" 탭의 캐시 무효화 패턴과 동일).
- **일일 리포트 통합 (`report_builder.py`)**: 요약 표 바로 아래에 "모의 투자 현황" 섹션 — 보유 중인 포지션이 있을 때만 티커/매수일·가/수량/**설정일**/현재가/미실현손익%(색상 표시) 표를 추가하고, 없으면 섹션 자체를 생략한다(HOLD 종목에 뉴스/분석이 없는 것과 같은 원칙). 청산 내역은 리포트에는 넣지 않는다(Streamlit에서만 확인). **설정일(`created_at`, v3.23)**: 매수일(`entry_date`, 소급 지정 가능한 시뮬레이션 상 거래일)과는 별개로, 그 포지션을 실제로 언제 기록했는지를 함께 보여준다 — 스키마는 처음부터 `created_at`을 저장하고 있었고 노출만 v3.23에서 추가됨. Streamlit "모의 투자" 탭의 보유 포지션 카드·청산 내역 표에도 동일하게 표시된다.
- **텔레그램 통합 (`telegram_notifier.py`)**: 기존 액션 요약 표 아래에, 보유 포지션이 있을 때만 같은 등폭 코드블록 형식으로 포지션 표(티커/매수일/매수가/현재가/수익률%)를 추가한다. 포지션이 없으면 이 블록 자체를 생략해 메시지가 불필요하게 길어지지 않게 한다.
- **크론 연결 (`recommendation_engine.run_asset_class_recommendations`)**: 리포트를 만들기 직전에 `paper_trading.load_positions`+`compute_position_returns`를 한 번 호출해 위 두 통합 지점에 그대로 전달한다(리포트/텔레그램이 각자 다시 계산하지 않음). 이 조회는 try/except로 감싸, 실패해도 이미 계산된 추천 결과 저장·리포트 생성·텔레그램 발송 중 어느 것도 막지 않는다.
- **v1 스코프에서 제외**: 부분 청산/스케일 인아웃, 시스템이 스스로 진입/청산하는 자동매매 연동(향후 별도 기능으로 가능), 포트폴리오 단위 합산 손익/샤프비율 같은 리스크 지표, 통화 환산(모든 대상 티커가 이미 USD 표시).

---

## 3. 코드 작성 시 주의사항 & 예외 처리 규칙

1. **API 할당량 관리**: yfinance 대량 호출 시 차단 위험을 방지하기 위해 종목 간 `time.sleep(0.5)` 등 미세한 딜레이를 부여할 것.
2. **Parquet I/O 최적화**: 구글 드라이브에서 매번 전체 파일을 다운로드하는 리스크를 줄이기 위해, 당일 조회 빈도가 높은 데이터는 Streamlit 백엔드의 내장 캐시(`st.cache_data`)를 적극 활용할 것. (`app.py`에 적용됨: `DriveDB` 연결은 `st.cache_resource`, 종목 목록/시세 조회는 `st.cache_data(ttl=3600)`, LLM 매매 추천은 `(티커, 최신 날짜)` 키로 캐싱)
3. **가짜 돌파 필터**: 가격 돌파 발생 시, 당일 거래량이 '최근 20일 평균 거래량의 1.5배 이상'인 종목만 대시보드 상단에 우선순위로 노출할 것.
4. **LLM 프로바이더 장애 대응**: OpenRouter 무료 티어 모델 호출이 실패해도 전체 배치가 중단되지 않도록, 종목별로 예외를 잡아 로그만 남기고 다음 종목으로 계속 진행할 것.
5. **자산군 ETF는 S&P 500 편입/편출 로직에서 항상 제외**: `sync_universe`가 참조하는 "저장된 티커" 집합에서 `ASSET_CLASS_TICKERS`를 빼고 diff할 것. 빼지 않으면 이 자산군들(SPY/QQQ/BTC-USD/GLD/TLT/IEF/DBC/USO/UNG/DBA/CPER/UUP)이 매번 "S&P 500에서 편출된 종목"으로 잘못 분류된다.
6. **매수/매도 판정은 절대 네트워크 호출에 의존하지 말 것**: 액션(매수/HOLD/매도)은 항상 `signal_engine.get_mechanical_action`(순수 계산, 네트워크 없음)으로만 결정한다. LLM/뉴스 API는 매수·매도 시그널이 발생한 날에만 서술형 설명을 덧붙이는 용도로만 쓰고, 그 호출이 실패해도 규칙 기반 설명(`recommendation_engine._build_rule_based_explanation`)으로 대체할 뿐 판정 자체를 HOLD로 낮추거나 유실시켜서는 안 된다.
7. **차트는 항상 클라이언트(브라우저) 사이드에서 렌더링할 것, 서버 쪽 헤드리스 이미지 렌더링을 다시 들이지 말 것**: v2.8~v2.9에서는 텔레그램 첨부용으로 kaleido(헤드리스 Chromium)를 썼다가 CI에 CJK 폰트가 없어 한글이 깨지는 문제를 겪었다. v3.0에서 `fig.to_html()` 기반 리포트 임베드로 전환하며 이 클래스의 문제 자체를 없앴으니, 향후에도 정적 이미지가 꼭 필요한 게 아니라면 굳이 kaleido/`to_image`를 다시 추가하지 말 것.
8. **일일 리포트 생성 실패가 크론의 나머지 단계를 막아서는 안 됨**: `report_builder.build_daily_report_html`/`save_report` 실패(LLM, Drive, Git 커밋·푸시 어느 단계든)는 예외를 잡아 로그만 남기고, 이미 확정된 매수/HOLD/매도 판정 저장과 텔레그램 발송은 그대로 진행할 것 — 리포트는 부가 산출물이지 판정의 전제조건이 아니다.
9. **LLM 교차 자산 총평은 개별 종목 판정에 관여하지 않게 분리할 것**: `openrouter_briefing.generate_portfolio_overview`는 해설 전용이며, 규칙 6과 마찬가지로 `signal_engine.get_mechanical_action`이 정한 개별 판정을 절대 바꾸거나 재해석하지 않는다. 시스템 프롬프트에 이 제약을 명시적으로 못박을 것.
10. **LLM/외부 API 응답을 HTML에 그대로 꽂지 말 것**: `report_builder.py`처럼 LLM 서술 텍스트나 Exa 뉴스 제목/요약을 f-string으로 HTML에 삽입할 때는 반드시 `html.escape()`(또는 동급 이스케이프)를 거칠 것 — 이 텍스트들은 우리가 생성한 게 아니라 외부에서 온 값이라, `<`/`>` 등이 섞이면 마크업이 깨지거나 의도치 않은 태그가 삽입될 수 있다.
11. **재사용 가능한 데이터를 다시 계산/재호출하지 말 것**: `recommendation_engine.get_recommendation_for_ticker`가 이미 만든 뉴스/LLM 분석 결과(`results[ticker]`)는 리포트·텔레그램·Streamlit 어디서 쓰든 그대로 재사용한다. 같은 데이터를 위해 Exa/OpenRouter를 다시 호출하는 코드를 추가하지 말 것 — API 사용량/비용/rate limit 관리 원칙([기능 5] 참고)과 직결된다.
12. **로컬 `.env`와 GitHub Actions Secrets는 완전히 별개 저장소임을 항상 기억할 것**: 하나를 바꿔도 다른 하나는 자동으로 갱신되지 않는다. 자격정보(예: `TELEGRAM_CHAT_ID`를 1:1에서 그룹으로 바꿀 때)를 바꾼 뒤에는 `gh secret set <이름>`으로 Secret도 반드시 같이 갱신할 것 — 안 그러면 로컬 테스트는 되는데 실제 크론(`recommend.yml`)은 예전 값으로 계속 도는, 알아채기 어려운 불일치가 생긴다. `gh secret list`로 각 Secret이 마지막으로 갱신된 시각을 확인해 `.env` 변경 시점과 대조하면 바로 알 수 있다.
13. **API 비용 없이 파이프라인만 확인하려면 `skip_llm_and_news` 입력을 쓸 것**: `recommend.yml`을 GitHub Actions 탭에서 수동(`workflow_dispatch`)으로 재실행할 때 `skip_llm_and_news`(기본값 `true`)를 체크하면 Exa/OpenRouter 호출 없이 규칙 기반 설명으로 대체되어, 리포트/텔레그램/GitHub Pages 배관만 빠르게 검증할 수 있다(`gh workflow run recommend.yml -f skip_llm_and_news=true`). 실제 뉴스/LLM 분석까지 확인하려면 `-f skip_llm_and_news=false`로 명시할 것 — 매일 자동으로 도는 스케줄 실행(`workflow_run` 트리거)에는 이 입력 자체가 없어 항상 실제 호출이 일어나므로 실서비스에는 영향이 없다. 같은 수동 실행에는 `is_test`(기본값 `true`, v3.20, v3.22·v3.27에서 범위 확장)도 함께 켜져 있어, 발행되는 리포트 파일명과 추천 결과 JSON(`_recommendations_{date}.json`) 파일명 양쪽 끝에 `_test`가 붙는다(`config.IS_TEST_REPORT`) — 그날의 실제 리포트/추천 아카이브를 덮어쓰지 않기 위함이니, 진짜로 그날 것을 다시 발행해야 하는 상황(예: 실패 후 재실행)이 아니면 `is_test=false`로 바꾸지 말 것. `is_test=true`인 발행은 **Drive에만** 저장되고 로컬 `docs/reports/` 파일 쓰기 자체를 건너뛰므로(v3.27) `recommend.yml`이 git에 커밋할 것도, GitHub Pages에 공개될 것도 없다 — v3.25~v3.26에서는 Streamlit 목록에서만 숨겼을 뿐 실제로는 여전히 커밋/공개되고 있었던 것을 v3.27에서 근본적으로 막았다.
14. **일목균형표(및 향후 추가될 참고 지표)는 규칙 6과 동일하게 어드바이저리 전용으로 취급할 것**: `signal_engine.get_ichimoku_confluence`가 반환하는 "구름과 판정이 일치하는지" 정보는 `recommendation_engine`의 서술 텍스트에 문장 하나를 덧붙이는 데만 쓴다 — `get_mechanical_action`의 반환값이나 `calculate_position_size`가 계산하는 수량을 이 정보로 조정하는 코드를 추가하지 말 것(사용자가 서술 코멘트만으로 충분하다고 명시적으로 확인한 설계). 새 참고 지표를 추가할 때도 이 분리(계산은 `signal_engine`, 서술 반영은 `recommendation_engine`/`openrouter_briefing`, 액션·수량 불변)를 그대로 따를 것.
15. **모의 투자(`paper_trading.py`)는 크론 경로에서 절대 쓰기(open/close)를 하지 말 것**: `_paper_trades.json`에 대한 `open_position`/`close_position` 호출은 Streamlit "모의 투자" 탭에서만 일어나야 한다 — `collect.yml`/`recommend.yml`은 `paper_trading.load_positions`/`compute_position_returns`로 **읽기만** 해서 리포트/텔레그램에 반영할 뿐, 포지션을 자동으로 열거나 닫는 로직을 크론에 추가하지 말 것(이 프로젝트의 크론은 처음부터 완전 무인·읽기 전용으로 설계돼 있다는 원칙과 직결). 이 읽기 자체가 실패해도 리포트/텔레그램/추천 결과 저장 중 어느 것도 막아서는 안 된다(규칙 6·8과 동일한 "이미 성공한 걸 놓치지 않는다" 원칙).
