__pycache__/
*.pyc
.venv/
venv/
.env
credentials.json
client_secret.json
token.json
*.parquet
*.log
.cache_ggshield

# prediction_model/ regenerable data artifacts (see prediction_model_spec.md) — only the
# .py scripts and the spec doc are tracked, same "local cache, not versioned" principle as
# triple_barrier_backtest_spec.md's backtest_cache/.
prediction_model/ohlcv_cache/
prediction_model/*.csv
prediction_model/*.npz
prediction_model/models/

# triple_barrier_backtest_spec.md's local-only cache (yfinance history + CSV summary) —
# regenerable, not versioned, never uploaded to Drive.
backtest_cache/
